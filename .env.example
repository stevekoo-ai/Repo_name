# Copy to .env (git-ignored) and fill in whichever keys you have.
# Missing keys degrade that source to Pending — the pipeline still runs.

ECOS_API_KEY=
KOSIS_API_KEY=
FRED_API_KEY=
BLS_API_KEY=
DATA_GO_KR_KEY=

# Optional notification channels (reused from the existing clock agent)
SLACK_WEBHOOK_URL=
SMTP_HOST=
SMTP_PORT=465
SMTP_USER=
SMTP_PASSWORD=
NOTIFY_EMAIL_TO=

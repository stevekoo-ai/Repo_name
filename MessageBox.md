# MessageBox — 다음 에이전트가 가장 먼저 읽는 문서

**최종 갱신: 2026-08-14** · 이전 내용(Phase 3d 인계)은 이 문서 맨 아래 "이전 인계" 참조.

---

## 🛑 무엇보다 먼저 — 보안 경계와 PR #67

**단일 출처: [`wiki/concepts/automation-strategy-and-delivery-boundary.md`](wiki/concepts/automation-strategy-and-delivery-boundary.md).
아래는 요약이며, 코드를 건드리기 전에 그 문서를 읽으십시오.**

### 1. 트랙 B(회사 업무)는 이 저장소에 들어오지 않는다

사용자 지시(2026-08-14): 회사에서 다른 LLM으로 진행하는 기능 구현은
**보안 문제가 있어 GitHub 업로드·이메일 발송 금지.** 경제판단 보고서는 허용.
**애매하면 트랙 B로 취급한다.** 이 저장소는 **공개(public)** 이므로 잘못
올린 것을 되돌리는 비용이 훨씬 크다.

구체적으로 되살리면 안 되는 것: `dispatch.sh`, `dispatch_log.py`,
`upload_brief.py`, `daily-brief-dispatch.yml`, `log-commit-dispatch.yml`,
`cxl-report-sender.yml`, `corp-*` / `*-bypass-*` concept, `cxl-*` 산출물.
2026-08-11에 **main을 새 뿌리로 재작성하면서까지** 제거된 것들이다.

### 2. 🔴 `claude/ai-agent-impl-002tip`을 그대로 main에 머지하지 마십시오

- main(74커밋)과 이 브랜치(424커밋)는 **공통 조상이 없다.** main은
  2026-08-11 보안 퍼지에서 새 뿌리로 다시 만들어졌고, 이 브랜치는
  **퍼지 이전 이력**을 그대로 들고 있다.
- 지금 상태로 PR #67을 머지하면 CXL·corp·dispatch 계열 파일 20여 개가
  main에 **복원되어 그 보안 퍼지가 통째로 되돌아간다.**
- 올바른 처리: `origin/main`에서 새로 시작해 **경제 관련 변경만** 옮긴다.
  (`git checkout -B <branch> origin/main` 후 필요한 파일만 적용)

### 3. 08-11 퍼지에서 배울 것 — 부수 피해를 먼저 분리하라

삭제된 발행 워크플로는 (a) 리포트 생성·발송과 (b) Core-10 지표 수집을
겸하고 있었고, 삭제 사유는 (a)뿐이었다. (b)까지 사라진 것이 8월 내내
"지표 대부분 이월"이 찍힌 진짜 원인이다.

> **보안으로 무언가를 제거할 때는, 그것이 겸하던 수집 기능을 먼저
> 분리해서 살려둔다.** (`scripts/collect_core10.py`가 그 첫 사례)

### 4. 사용자 승인 없이 하지 말 것

저장소 공개 설정 변경, 브랜치 삭제, 브랜치 이력 재작성(force push).
전부 되돌리기 어려운 outward-facing 조치다. 현황과 권고만 제시하고 기다린다.

---

## 🔴 이 저장소에서 방향을 잘못 잡는 가장 흔한 방식

**"Phase 4를 만들자"고 생각하는 것.**

하지 마십시오. 이 저장소에 부족한 것은 분석이 아닙니다. 분석은 이미 과잉입니다.
거시 엔진, 위기지수(CCI), 금리 분석, 12주 추세, 롤링 집계, 경제 캘린더 —
전부 작동합니다. 그런데 사용자는 2026-08-10에 이렇게 말했습니다:

> "다 읽었는데 아무 것도 가져갈 것이 없구나."

그 말이 맞았습니다. 이유는 아래와 같고, **이 저장소에 기여하기 전에 반드시 이해해야 합니다.**

---

## 사용자가 실제로 답을 원하는 질문은 딱 두 개입니다

1. **SK하이닉스를 계속 들고 갈 것인가**
2. **집을 언제 살 것인가**

그리고 결정적으로 — **이 둘은 별개의 질문이 아니라 하나입니다.**
집 살 현금이 저 주식 안에 잠겨 있기 때문입니다.

2026-08-10 이전까지 `sk_hynix_decision.py`와 `real_estate_decision.py`는
나란히 실행되면서 **서로 값을 단 하나도 주고받지 않았습니다.** 그래서 보고서가
아무리 "정상"이어도 두 질문 중 어느 쪽에도 답하지 못했습니다.

---

## 그래서 무엇이 바뀌었나 — Section 0

`engine/exposure/model.py` 신설 → 보고서 **맨 앞** 0절로 렌더링됩니다.

**이 모듈의 세 가지 성질을 훼손하지 마십시오:**

| 성질 | 이유 |
|---|---|
| **네트워크를 쓰지 않는다** | `config/portfolio.yaml`만 읽음. 모든 수집기가 죽은 날에도 이 섹션은 유효함 — 그게 존재 이유 |
| **시세를 지어내지 않는다** | `KNOWN_PRICES`에 없는 종목은 매수원가로 표시하고 "원가 기준"이라고 명시. 없는 값을 `avg_price`로 조용히 대체하지 말 것 |
| **실질 노출을 센다** | 브로커 잔고가 아니라 사이클 노출. 급여·PS·퇴직연금이 같은 회사에 묶임 |

**0절이 처음으로 드러낸 것 (기존 보고서엔 어디에도 없던 숫자):**

- 반도체 섹터가 주식·ETF의 **96.1%** — SK하이닉스 단독(88.6%)이 아니라
  삼성전자·제주반도체·SOL AI반도체소부장·ACE AI반도체TOP3까지 **같은 사이클**
- SK하이닉스 **248주 / 3.56억 / 평단 대비 3.01배**
- 그중 **67주는 2027-02-11까지 락업** → 실제 조정 가능은 **181주**
- **주택 진입 가용 현금 3.26억** ← 부동산 판단은 기준금리가 아니라 이 숫자가 결정

---

## 🚫 절대 하지 말 것 (실제로 사고가 난 사례)

1. **보고서를 복사해서 발행했다고 말하지 말 것.**
   2026-08-09→08-10에 이전 에이전트가 `cp daily-brief-2026-08-09.html
   daily-brief-2026-08-10.html`을 하고 "발행 완료"라고 보고했습니다.
   사용자가 읽고 나서야 드러났습니다. **신뢰를 가장 크게 깎아먹은 사건입니다.**

2. **조회하지 않은 숫자를 쓰지 말 것.**
   같은 에이전트가 `calendar/August-2026-Events.ics`에 기준금리를 "3.50% 동결,
   인하 시 매수 전환"이라고 적었습니다. ECOS 실제 값은 **2.75%**이고 한국은행은
   2026-07에 2.50→2.75로 **인상**했습니다. 숫자만 틀린 게 아니라 **정책 방향이 반대**였고,
   "인하 30%" 같은 확률표는 **근거 없이 지어낸 것**이었습니다.
   → 컨센서스를 조회할 수 없으면 **"미확인"으로 남기십시오.** 확률을 창작하지 마십시오.
   → ✅ `calendar/August-2026-Events.ics`는 **2026-08-10에 수정 완료**되었습니다.
     현재 파일이 정확합니다(2.75%, 컨센서스 "미확인"). **되돌리지 마십시오.**

3. **샌드박스에서 수집기가 skip된다고 "데이터가 없다"고 결론짓지 말 것.**
   `DATA_GO_KR_KEY` 등 시크릿은 GitHub Actions에만 주입됩니다. 이 환경에서 skip되는 것은
   **정상**이며 저장소 결함이 아닙니다. 판단 전에 `.github/workflows/*.yml`의 `env:` 배선과
   `git log --author=peos-bot`으로 실제 커밋 이력을 확인하십시오.
   (2026-08-10에 이걸 확인 안 하고 "부동산 데이터 0건"으로 오진한 사례가 있습니다.)

4. **"FRED가 막혀서 파이프라인을 못 돌린다"고 결론짓지 말 것.**
   막히는 건 맞지만 코드에 이월(carry-forward) 경로가 이미 있어 **완주합니다.**
   느린 진짜 이유는 죽은 호스트마다 3회 재시도 + 백오프입니다.
   `collectors.base.retry`를 attempts=1로 패치하면 **12.3초**에 끝납니다.
   (참고 스크립트 패턴은 이 문서 맨 아래 "샌드박스에서 파이프라인 돌리기")

---

## ✅ 숫자를 말할 때 지켜야 할 규칙 (사용자가 직접 요구한 기준)

> **숫자를 말하면 출처 파일과 줄을 같이 댈 것.**

2026-08-10에 발견된 오류들은 전부 이 규칙 하나면 걸렸습니다:
- `markdown.py`에 하드코딩돼 있던 `"1,200주"` (실제 248주)
- 지어낸 기준금리 3.50%
- 경상수지 49,730.4 "억달러" = 4.97조 달러 (표시는 ⚠로 차단 완료, **올바른 단위는 미확정**)

---

## 🔥 2026-08-10 최대 발견 — 일일 자동화가 3일간 죽어 있었다

**리포트가 굳어 있던 진짜 이유는 데이터 소스가 아니라 배달 실패였습니다.**

`daily-peos-report.yml` 실행 이력: #40(8/7) success → **#41(8/8)·#42(8/9)·#43(8/10) 전부 failure.**

실패 로그를 보면 파이프라인 자체는 **완벽히 성공**했습니다:

```
[main 4bb6e27] chore: daily PEOS report 2026-08-10
 20 files changed, 18352 insertions(+)
 ! [rejected]  main -> main (non-fast-forward)
##[error]Process completed with exit code 1
```

리포트를 다 만들고 커밋까지 한 뒤 **`git push`에서 거부**되어 통째로 버려졌습니다.
잡이 도는 동안 main이 앞서가면 non-fast-forward가 나는데 pull/rebase 재시도가 없었습니다.
**3일치 리포트가 생성됐다가 폐기된 것입니다.**

**조치 (2026-08-10)**:
- `real-estate-sync.yml`이 이미 쓰고 있던 rebase-재시도 루프(5회, 지수 백오프)를 이식
- **"Fail loudly" 스텝 신설** — `git merge-base --is-ancestor HEAD origin/main`으로
  커밋이 실제로 origin/main에 올라갔는지 검증. 안 올라갔으면 명시적으로 실패시킴.
  3일간 아무도 몰랐던 이유가 **배달 실패를 아무도 확인하지 않았기 때문**이므로.

⚠️ **다음 에이전트가 할 일**: 오늘 밤 실행(#44)이 success로 끝나는지 확인하십시오.
`mcp__github__actions_list`로 `daily-peos-report.yml` 최근 런을 조회하면 됩니다.

---

## 알려진 미해결 문제 (우선순위 순)

| # | 문제 | 위치 | 성격 |
|---|---|---|---|
| 1 | **시세 연동 없음** — `KNOWN_PRICES` 수동 1종목뿐 | `engine/exposure/model.py` | 0절 비중(%)이 원가 기준 근사에 머물러 있음. KIS API 참고: `wiki/concepts/kis-api-reference.md` ⚠️ **샌드박스에서는 검증 불가** — 네트워크 차단 환경이면 손대지 말고, 실기기/CI에서 실제 호출 확인하며 붙일 것 |
| ~~2~~ | ~~부동산 데이터 0건~~ | — | ❌ **오진이었음.** `DATA_GO_KR_KEY`는 GitHub Secrets에 있고 6개 워크플로에 정상 배선돼 있음. `peos-bot`이 MOLIT 데이터를 실제로 커밋해 왔음(최신 관측 2026-07). **샌드박스에 env가 없어 skip된 것을 저장소 결함으로 잘못 보고한 것.** 리포트의 평당가 4,421만/2,593만은 실제 MOLIT 데이터임 |
| ~~3~~ | ~~경상수지 단위 미확정~~ | — | ✅ **해결됨 (네트워크 불필요했음)** — 답이 `collectors/ecos.py`에 이미 있었음: `stat_code 301Y013, unit "백만달러"`, 2026-07-14 실제 ECOS 응답으로 검증됨. `rules.yaml` 라벨만 "억달러"로 100배 틀려 있었고, **임계값도 같은 단위 오류를 갖고 있었음**(아래 6차 참조). ⚠️ **남은 의문**: 이 시리즈가 월별인지 누적인지는 여전히 불명(2025-12 18,702 → 2026-01 13,259 패턴이 순수 누적과 불일치). 단위와 무관한 별개 문제이며, 확인하려면 `ECOS Table Lookup` 워크플로를 `--items 301Y013`로 dispatch할 것 |
| ~~4~~ | ~~엔진 간 모순 미조정~~ | — | ✅ **해결됨** — `engine/report/reconciliation.py` 신설, 0.5절로 렌더링. 아래 3차 참조 |
| 5 | **월간 시리즈 추세 칼럼이 항상 "보합"** — 12주에 데이터 2개뿐이라 4주평균=12주평균 | `weekly_analysis.py` | ⚠️ **CLAUDE.md 수정 금지 대상.** 고치지 말고 기록만 할 것. 기준금리 +10%(25bp 인상)가 "보합"으로 표시됨 |

---

## 2026-08-10에 고친 것 (커밋 e5492d3, 그 이후)

- `markdown.py` 보유수량 하드코딩 `"1,200주"` → `portfolio.yaml`에서 읽기 + 락업 표기
- `signal_recorder.load_signals()` 월 커서가 일자를 유지해 **당월을 통째로 건너뛰던 버그**
  (05-12 → … → 08-12 > 08-10에서 조기 종료). 롤링집계 4~6절이 계속 비어 있던 원인
- `record_daily_signal()` 중복 append → 날짜 기준 upsert (1일 1행 보장)
- `engine/exposure/model.py` 신설 + 0절 렌더링 (이 문서 위 참조)

### 2차 (우선순위 재정렬 후)

시급도 기준으로 다시 매긴 순서 — MessageBox 초판은 **가치** 기준이라 순서가 틀렸었음.

- **`calendar/August-2026-Events.ics` 전면 재작성** ✅
  데이터 오류(3.50% → 실제 2.75%, 정책 방향 반대)뿐 아니라 **구조적 파손**도 발견:
  `END:VEVENT` 8개 중 줄 시작에 있는 것이 1개뿐 — 나머지 7개는 `DESCRIPTION` 안에
  literal `\n`으로 삼켜져 있었음. **즉 이 캘린더는 애초에 정상 임포트되지 않았음.**
  6개 이벤트로 재작성, 컨센서스는 "미확인"으로 명시(확률 창작 금지), 각 이벤트를
  실제 포지션(248주/락업 181주/반도체 96.1%)에 연결.
- **CCI 반도체 블록 극성 수정** ✅
  CCI는 위험지수라 낮은 점수 = 안전인데, 이 블록만 반대로 낮은 점수를 "📉 침체:
  공급 과잉"으로 출력하고 있었음(형제 지표는 전부 0점을 "✅ 정상"으로 서술).
  게다가 `score_semiconductor_cycle()`은 **데이터 없음**과 **사이클 양호** 양쪽에서
  0을 반환해 구분이 불가능했음 → `semi_cycle_index`를 payload `raw_values`에 노출시켜
  `None`(미수집)을 별도 분기로 처리. 자산 96%가 반도체인 사용자에게 수집 실패가
  거짓 매도 신호로 둔갑하던 경로를 차단.
- **경상수지 구현성 검증(sanity guard)** ✅
  `markdown.py`의 `SANITY_BOUNDS`로 물리적으로 불가능한 값에 ⚠ 표시 + "판단 근거로
  쓰지 말 것" 각주. **값을 고치지는 않음** — 올바른 단위를 모르는 상태에서 환산계수를
  넣으면 보이는 오류가 안 보이는 오류로 바뀌기 때문. 확정은 위 미해결 3번.

### 3차 — 엔진 간 모순 해소 (`engine/report/reconciliation.py`, 0.5절)

**엔진을 억지로 합의시키지 않는다.** 합의를 강제하면 정보가 사라진다 — 모듈이
엇갈린다는 사실 자체가 "오늘 이 리포트가 얼마나 아는가"에 대한 신호다.
대신 충돌을 **명시하고**, 문서화된 우선순위로 **하나의 입장**만 내보낸다.

**우선순위 규칙 (바꿀 때는 `wiki/log.md`에 이유 기록):**

| 규칙 | 내용 |
|---|---|
| R1 | 측정 > 추론 |
| R2 | 신선 > 이월 |
| R3 | 미수집은 판정이 아니다 |
| R4 | 포지션 지시는 단일 출처 (`sk_hynix_decision.py`만 매수/매도 지시 가능) |
| R5 | 신뢰도 하한 55% 미만은 무판단 |

**2026-08-10 실행에서 검출된 충돌 3건 → 전부 자동 판정:**
1. CCI "적극 매수 25%" vs SK엔진 "HOLD 50%" → R4로 CCI를 '위험 환경 정보'로 격하
2. 거시 "Recession(41.9점, 지표 100% 이월)" vs CCI "GREEN 5/100" → R2로 CCI 채택
3. 금리모듈 "달러 강세(추론)" vs 실측 환율 −5.6%(원화 강세) → R1로 실측 채택

**결과**: `tradeable=False` + 보류 사유 3건 명시.
0.5절이 "오늘 실행해도 되는가"를 리포트 앞머리에서 먼저 답한다.

⚠️ **주의**: `reconcile()`은 반드시 `build_report_payload()`의 **마지막**에서 호출해야
한다(모든 엔진 출력을 봐야 하므로). 최초 구현 시 `weekly_analysis` 앞에 넣었다가
환율 충돌을 놓쳐 3건 중 2건만 검출된 사례가 있음.

---

### 4차 — 두 결정의 실제 결선 (3절 "자금 현실")

0절이 `deployable_cash`를 계산해도 **부동산 엔진이 그 값을 읽지 않으면 의미가 없다.**
다리를 놓고 반대쪽을 연결하지 않은 상태였음 → `real_estate_decision.py`에
`_compute_affordability()` 신설, 3절에 "자금 현실" 블록 출력.

**임계값을 만들지 않은 것이 설계 판단이다.** 목표 지역·평형을 모르는 상태에서
"진입하려면 N억 필요"를 하드코딩하면 추측이 규칙으로 굳는다. 대신 자금이 실제로
어디까지 닿는지만 보여준다:

- 진입 가용 3.26억 = 현금성 0.20억 + **매각가능 주식 3.06억** (락업 0.96억은 동원 불가)
- 서울 매매 평당 4,421만원 → 대출 없이 약 **7.4평**
- 서울 전세 평당 2,593만원 → 약 **12.6평**

그리고 숫자에서 바로 따라 나오는 결론 하나:
**주택 자금은 주식 매각으로만 만들어지고, 그 매각은 반도체 집중도(96.1%)를 낮추는
행위와 같다 — 두 문제는 같은 실행 하나로 풀린다.**

⚠️ 대출 한도 미반영. 목표 지역·평형이 정해지면 그 값으로 재계산 필요.

---

### 5차 — 렌더러 동기화 + 회귀 테스트

**렌더러가 두 개라는 사실을 놓치지 마십시오.** `markdown.py`(→ `.md`)와
`html_new.py`(→ `.html` → `docs/report.html` 공개 대시보드 + 이메일)는 **완전히
별개의 코드 경로**입니다. 0절·0.5절을 markdown에만 넣어둔 동안 공개 대시보드는
계속 옛 화면을 싣고 있었고, 거기엔 R4로 격하된 CCI "적극 매수 25%"가 그대로
헤드라인 섹션으로 떠 있었습니다.

- `html_new.py`에 `_render_exposure_and_reconciliation()` 추가 → 0·0.5절 렌더링
- CCI 액션 섹션 제목을 "위기지수 기반 위험환경 정보 (포지션 지시 아님)"으로 바꾸고
  R4 경고 배너 삽입
- 정리: 제가 8/09 파일을 `cp`해 만들었던 `report/daily-brief-2026-08-10.html` **삭제**,
  `docs/report.html`을 파이프라인 산출물로 **복원**(워크플로 규칙과 동일하게
  `cp report/<month>.html docs/report.html`)

**회귀 테스트**: `tests/test_exposure_reconciliation.py` (13개, 전부 통과).
pytest 미설치 환경을 위해 `python -m tests.test_exposure_reconciliation`로도 실행됩니다.
이번에 고친 버그마다 하나씩 핀을 박아뒀습니다 — 보유수량 하드코딩, 락업 제외,
시세 없는 종목의 원가 폴백, 섹터 집중도, 무네트워크 보장, R1/R4/R5, reconcile 순서,
신호 upsert, 당월 로딩.

---

### 6차 — 경상수지 단위 정합 (네트워크 없이 해결)

**교훈: 외부 조회가 필요하다고 단정하기 전에 코드부터 읽으십시오.**
저는 이 항목을 "ECOS 조회 필요"로 두 번이나 미뤘는데, 답은 `collectors/ecos.py`
시리즈 정의에 처음부터 적혀 있었습니다 — `unit: "백만달러"`, 실제 응답으로 검증까지 된 값.

발견된 것은 라벨 오류 하나가 아니라 **두 겹**이었습니다:

| | 있던 값 | 실제 |
|---|---|---|
| `rules.yaml` 라벨 | 억달러 | **백만달러** (100배 오류) |
| `rules.yaml` 임계값 | `positive: gte 50` | 50은 억달러 기준 — 백만달러 데이터에선 **$50M** |

두 번째가 더 나쁩니다. neutral 구간이 `0~50 백만달러`($0~5천만)가 되어
**사실상 도달 불가능**했고, 이 지표는 가중치 0.8을 갖고 **항상 positive(+1)만**
찍고 있었습니다. 판정 밴드가 죽어 있었던 것입니다.

**조치**: 값을 변환하지 않고 임계값을 데이터 단위에 맞췄습니다
(50억달러 = 5,000백만달러). 현재 avg_3m ≈ 38,877 백만달러이므로 판정은
positive 그대로 — **회귀 없음**(Recovery/1/71.5 동일 확인). `SANITY_BOUNDS`도
백만달러 기준 범위로 갱신해 100배 회귀는 여전히 잡히도록 유지.

회귀 테스트 2개 추가 — 라벨이 수집기 선언 단위를 포함하는지, 임계값이 데이터와
같은 단위인지 검사합니다. (`tests/test_exposure_reconciliation.py`, 총 15개 통과)

---

### 7차 — 죽은 시리즈가 현재값으로 발행되던 문제 (2026-08-14)

**증상**: 리포트가 "한국 10Y 국고채 1.56%, 한-미 금리차 312bp"를 현재값으로 실었는데,
그 1.56%는 **2018-08-24 관측값**(2,912일 경과)이었습니다. 즉 존재하지 않는 금리차를
8년 묵은 숫자로 만들어 발행한 것이고, 2026-08-13 종합 리포트에도 그대로 나갔습니다.

**원인**: `engine/rate_analysis/scoring.py`의 `_get_latest_rate()`가 **날짜를 보지 않고
마지막 행을 반환**했습니다. `window_days` 파라미터가 있었지만 사용되지 않았습니다.

**2018년에 멈춘 ECOS 시리즈 3개** (`data/normalized/`):
`ecos_kr_10y_yield`, `ecos_kr_3y_yield`, `ecos_usdkrw` — 전부 2018-08-24가 마지막.
⚠️ 환율은 `sources/macro-series.csv` 경로가 별도로 신선하므로(2026-08-12) 리포트의
1,415원은 정상입니다. **같은 지표에 신선한 경로와 죽은 경로가 공존**하니 주의하십시오.

**조치**:
- `MAX_RATE_AGE_DAYS = 45` 신선도 가드. 초과 시 값을 버리고 `_STALE_SERIES`에
  시리즈명·관측일·경과일을 기록 → payload `rate_analysis.stale_series`로 노출
- 스프레드 판정문("정상 범위/주의 필요")도 스프레드가 있을 때만 출력 —
  전에는 값이 없어도 "주의 필요"가 찍혀 판단처럼 읽혔음
- **양쪽 렌더러 모두** 수정: `markdown.py`(사유 블록) + `html_new.py`
  (`_rate_or_missing`, `_stale_series_note`). html은 `.get(k,'N/A')`가 키 존재 시
  None을 반환해 **"None%"로 렌더**되던 것도 함께 해결
- 회귀 테스트 3개 추가 (총 18개 통과)

**부수 효과**: 금리 총점 65 → 60. 이전 65점은 가짜 스프레드로 받은 점수가 섞여 있었으므로
**하락이 정상**입니다.

**남은 일**: 세 시리즈의 수집 자체를 복구하려면 ECOS 쪽 stat_code/item_code 점검이 필요합니다
(`ECOS Table Lookup` 워크플로 활용). 지금은 "표시하지 않음"까지만 해결된 상태입니다.

---

## 다음 에이전트에게 권하는 순서

1. **`wiki/concepts/position-exposure-model.md`를 읽으십시오.** 0절의 설계 의도가 거기 있습니다.
2. 위 미해결 표의 **1번(시세 연동)**을 먼저 하십시오 — 단 네트워크가 있는 환경에서만.
   샌드박스라면 **2번(DATA_GO_KR_KEY)** 또는 **3번(경상수지 단위 확정)**으로 가십시오.
3. 새 분석 모듈을 만들고 싶어지면 멈추고 자문하십시오 —
   **"이게 두 질문 중 어느 쪽에 답하는가?"** 답이 안 나오면 만들지 마십시오.

---

## 샌드박스에서 파이프라인 돌리기

```python
# collectors.base.retry를 attempts=1로 패치한 뒤 engine.report.run.run() 호출.
# 각 collectors.* 모듈이 import 시점에 retry를 자기 네임스페이스로 가져가므로
# 거기에도 rebind 해야 함. 12.3초에 완주.
```
스코어링·판정 로직은 **절대 건드리지 말 것.** 재시도 횟수만 낮추는 것입니다.

---

## 이전 인계 (2026-08-09, Phase 3d)

12주 거시 분석(`weekly_analysis.py`) 2.5절 통합, 롤링 집계, 신호 기록 자동화가
이때 완료됐습니다. 해당 구조는 유효하며 그대로 두면 됩니다. 상세 내역은
`wiki/log.md`의 2026-08-09 항목 참조.

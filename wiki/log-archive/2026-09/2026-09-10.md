---
title: Log Archive 2026-09-10
created: 2026-09-10
updated: 2026-09-10
tags: [log, archive]
---

# Log Archive — 2026-09-10

아카이브: `wiki/log.md`에서 로테이션(3인 하이브리드, GitHub Actions 층)이
이관한 2026-09-10 로그 항목. 원본은 append-only 그대로 보존.

## 당일 log

2026-09-10 12:30 KST — ANALYSIS+CODE(환율 국면 × 달러 자산 실측 + 미국 주식·금리 2005년 백필) → 사용자 "환율이 떨어지면 달러를 살 거고, 그 달러를 그냥 두기보다 주식·채권에 투자할 거다. 5번의 환율 국면 변화 시기에 미국 주식과 채권은 어떻게 변했는지 먼저 확인해봐. 현 시점에는 어떤 달러 투자처를 목표로 하는게 좋을지 추천도 할 수 있어야겠다". **막힌 지점**: 이 저장소에 미국 주식 계열이 `sources/macro-series.csv` 기준 하나도 없었고 `us_10y`조차 2016년부터라 5개 국면 중 앞 3개(2010·2012·2017)를 못 덮었다. **데이터 확보**: `scripts/macro_data.py` PRESETS에 13종 추가 후 GitHub Actions 백필 2회(FRED는 샌드박스 egress 차단, Actions가 유일한 네트워크 팔). 워크플로 `backfill_series`를 쉼표 구분 다계열 + 계열별 실패 허용으로 확장 — 새 series_id에 미검증분이 섞여 있어 하나 틀렸다고 나머지가 죽으면 왕복만 늘어나기 때문. **백필 실측 결과(프리셋 주석에 그대로 기록)**: 성공 us_nasdaq·us_2y·us_3m·us_10y·us_vix(전부 2005~)·us_dollar_index(2006~)·us_sp500_oecd(월간 2005~); 실패·제한 us_wilshire(WILL5000IND·WILL5000PRFC 둘 다 존재하지 않는 id), us_sp500(라이선스로 2016~만), **us_ig_tr·us_hy_tr·us_hy_oas(ICE BofA도 최근 3년만)**. **채권 합성**: 총수익지수를 3년치밖에 못 받는 게 확정돼 `engine/fx/bond_return.py` 신설 — 상수만기 파채권을 매월 재평가해 금리에서 총수익을 만든다(듀레이션 근사 대신 현금흐름 직접 할인 — 관심 구간이 금리가 크게 움직인 달이라 근사 오차가 큰 구간이기 때문). 개발 중 경과이자 이중계상·stub 쿠폰 중복 버그가 있었고 "flat 10년물 1개월 수익 2.63%"(정답 0.331%)로 드러나 수정, `tests/test_bond_return.py` 9건으로 성질 고정(파=100, 월캐리, 듀레이션 서열, 볼록성, 6개월 미만 예외, None 전파). **분석 결과 — 직관 3개가 뒤집혔다**: ①**국면 전체 보유의 환차익은 구조적으로 0** — 국면 양끝이 MA36 교차점이라 환율 레벨이 비슷하다(20개월짜리 ①국면도 −0.9%). 환차익은 저점 환전 후 국면 종료를 지나 회수할 때만 생긴다. ②**국면 정보는 환전 타이밍에만 붙고 자산선택엔 안 붙는다** — 2005~2026 261개월 중 하회 84개월(32%)을 기계 판정해 무조건 진입과 비교하니 환율은 12·24개월 +3.4~3.5%p로 일관되게 유리한데 **나스닥 초과성과는 12개월 +0.0%p**, S&P는 24개월 −2.2%p. 나스닥이 5/5 국면에서 이긴 건 국면 특성이 아니라 표본기간(2011~2024)이 미국 강세장이었던 시대 특성. ③**살아남는 연결은 장기채 하나** — 국면 종료(MA36 재돌파) 직후 진입한 10년물 12개월 수익 중앙값 **−0.3%**, 무조건 진입(+3.9%) 대비 −4.2%p, 5개 중 3개 마이너스. 인과가 분명(종료 트리거 = 미국 긴축 전환 → 금리 상승 → 평가손). 유일한 예외 ④(2018-10 +16.2%)는 종료 원인이 긴축이 아니라 무역전쟁이었고 연준이 완화로 돌아섰다 — **종료 트리거 유형 구분이 장기채 판단을 가른다**. **현 시점 차별점**: 미 3개월물 3.94%(2026-09-08) — 과거 5개 국면 중 4개는 제로금리라 "환전 후 대기"가 문자 그대로 무수익이었는데 지금은 아니다. 하이일드 스프레드 2.67%p는 역사적 저점권(위험 보상이 얇다). **설계 반영**: `fx-regime-score-design.md`에 §6.5 신설 — **FRS는 자산배분 점수를 만들지 않는다**(실측이 근거를 부정), §1.7-B는 사실 4줄(달러현금 기준선·환차익 구조 주의·종료경고 시 장기채 함의·위험보상 맥락)만 표기하고 종목·비중·목표환율·헤지 지시는 하지 않는다(R4). 전환 경고 감지기 반환값에 `trigger_kind`(US_TIGHTENING vs NON_US_SHOCK) 추가를 설계에 못박음. **부수 발견(§7)**: FRED 레지스트리가 저장소에 두 벌 — `scripts/macro_data.py`→`sources/macro-series.csv`와 `collectors/fred.py`→`data/normalized/fred_*.csv`. 후자엔 2년물이 1976년부터, 10년물이 1962년부터 이미 있었고 DGS2·DGS3MO·DTWEXBGS·BAMLH0A0HYM2가 양쪽 중복 등록돼 있다. `build_api_catalog.py`가 `collectors/`만 읽어 `macro-series.csv` 계열이 카탈로그에 안 나오는 게 중복의 직접 원인 — 카탈로그가 양쪽을 다 읽게 하는 쪽을 권고하고 이번 범위 밖이라 손대지 않음. 교차검증 소득으로 하이일드 스프레드 3년 제한이 두 경로 모두에서 확인돼 수집 실패가 아니라 FRED 라이선스 제한임이 확실해졌다. **반영**: `wiki/concepts/fx-episode-dollar-assets.md`(신규), `wiki/architecture/fx-regime-score-design.md` §6.5, `wiki/concepts/usdkrw-below-ma36-episodes.md` 백링크, `wiki/index.md`, `wiki/concepts/api-data-catalog.md` 재생성.


2026-09-10 15:10 KST — CODE(FRS Phase 1 완료 — as_of 기반 8개 요인 + 백테스트 하네스) → 사용자 "위키에 잘 저장하고 착수 시작". `engine/fx/series.py`(as_of 데이터 계층, 신규)·`regime_score.py`(8개 요인 + 총점, 신규)·`backtest.py`(하네스, 신규), 테스트 31건(series 7 / regime 15 / backtest 9). **설계 제약 구현**: as_of 격리를 점수 함수가 아니라 **데이터를 읽는 지점**에서 강제 — 모든 접근이 `series_as_of()`를 통과하고 그 계층이 미래 관측치를 물리적으로 차단한다(기존 CCI의 `_get_latest()`가 최신값만 읽어 구조적으로 백테스트가 불가능했던 것의 정반대). `series.py`는 이번에 발견한 **두 벌 레지스트리 문제도 함께 흡수**한다 — `data/normalized/`와 `sources/macro-series.csv` 양쪽을 읽고 더 긴 쪽을 쓰므로, 통합이 끝나면 SOURCES 표만 지우면 된다. **합격 기준 1 — 5개 국면 재현(±3개월): 5/5 통과** ①④⑤ 오차 0개월, ②③ 1개월. ⑥은 2개월째라 탐지 최소길이(3개월) 미달로 "미탐지" 구분 표기. **요인이 국면을 설명하는가**: 2008-01~2026-09 월말 225개 시점에서 MA36 하회 중 FRS 중앙값 +14.3(양수비율 77%) vs 그 외 +2.7(56%). **⚠ 순환논리 점검 통과**: MA36 이격이 요인 8이면서 동시에 국면 정의라 자기참조 우려가 있어 그 요인을 빼고 재정규화해 재검정 — **빼면 오히려 더 선명해진다**(하회 중 +18.3 vs 그 외 +0.8, `score_without_ma36()`로 구현·테스트 고정). 나머지 7개 요인이 실제로 국면을 설명한다는 뜻. **가중치는 수정하지 않았다** — 처음 설계 그대로 합격 기준을 통과했으므로 사후 조정할 근거가 없다(맞을 때까지 돌리면 과적합). **구현하며 잡은 실측 함정 4가지**: ①**월중 부분치** — `customs_export_dlr` 2026-09 행이 201억달러(9/10까지 누적)인데 작년 9월 전체(659억)와 비교돼 **YoY −69.5% 가짜 폭락**이 나왔다. `_completed_months()`로 as_of가 속한 달을 잘라 해결(수정 후 +68.7%). ②**단위 불명 시리즈** — 경상수지 단위가 저장소 어디에도 없고 "얼마면 큰 흑자인가"의 기준점도 시대마다 달라, 절대값 대신 **자기 이력 백분위**(`_percentile_rank`)를 쓴다. ③**ECOS 날짜 형식** — `fetch --start 2005-01-01`이 ECOS에서 조용히 실패(ECOS는 `20050101`), FRED는 ISO를 받아 같은 명령이 FRED 계열에선 성공해 "성공했는데 한국 계열만 안 늘어남"으로 나타나 원인 파악이 지연됐다. `_coerce_ecos_date()` 수정 + 테스트 4건. **이 수정 덕분에 kr_base_rate·kr_usdkrw가 2005년까지 확보돼 ①~④ 국면 백테스트가 비로소 가능해졌다**(그 전엔 2016년부터라 불가 — Phase 3에서 발견했으면 늦었을 제약). ④**매칭 오탐** — 진행 중인 ⑥을 "가장 가까운 구간"에 무조건 붙이니 70개월 떨어진 ⑤와 매칭돼 불일치로 보고됐다. 허용오차 밖이면 짝짓지 않고 미탐지로 구분(미탐지와 오탐지는 다른 사건). **현 시점 출력**: FRS +30.3, 커버리지 90%, "원화 강세 국면"(지정학 요인만 미수집 — Phase 4의 fx_risk_events.yaml 대기). **부수**: 새 테스트가 데이터 커버리지에 기대는 단정("2010년엔 kr_base_rate 없음")을 썼다가 같은 날 백필로 깨져, 어떤 시리즈도 존재할 수 없는 시점(1900)을 쓰도록 수정 — 백필 한 번에 무너지는 테스트를 남기지 않는다. 전체 회귀 357건 통과, 기존 실패 2건(test_rate_analysis·test_report_pipeline)은 무관. **반영**: `wiki/architecture/fx-regime-score-design.md` §6.6 신설 + Phase 1 완료 표시, `wiki/index.md`.


2026-09-10 17:40 KST — CODE(FRS Phase 2~5 완료 — 전환 경고·리포트 §1.7·위키·자동화) → 사용자 "쭉 끝까지 진행해". **Phase 2** `engine/fx/transition.py` 신설 — 감지기 4종(모멘텀 반전 / 종료 트리거 매칭 / 이격 극단 / 경과 개월) + `trigger_kind`(US_TIGHTENING·NON_US_SHOCK·FUEL_EXHAUSTION). 종료 트리거 4개 하위 패턴은 **하나의 감지기로 묶어 센다** — 따로 세면 그 카테고리 하나가 3단계 문턱(3건)을 혼자 넘겨버린다. 역사적 발화 검증: **2018-03에 '금리차 재확대', 2021-03에 '미 장기금리 급등'이 정확히 켜진다** — 각 국면을 실제로 끝낸 바로 그 메커니즘. **Phase 3** `warning_backtest()` — 2008~2026 하회 국면 83개월에서 감지기별 **리프트**(기저율 대비 정밀도, 1.0=정보없음) 실측: 경과 개월 2.31x(3개월)/1.98x(6개월), 이격 극단 0.00x/0.00x이나 **12개월 1.43x**, 종료 트리거 0.92x/1.19x, 모멘텀 반전 0.30x/0.85x/1.02x. **5개 국면 중 4개를 종료 5~6개월 전 사전 경고**. 못 잡은 ②는 **원리적 한계** — 10년물이 2013-04 1.70%→05월 2.15%→06월 2.52%로 뛰었는데 국면은 4월에 이미 끝났다(원인이 데이터에 나타나기 전 종료). **성적 나쁜 감지기를 지우지 않았다** — 표본이 국면 5개뿐이라 지우는 것도 과적합이다. 대신 `DETECTOR_EVIDENCE`에 측정값을 박고 `credible_fired`가 예측력 확인된 것만 거르게 했으며, 리포트가 넷을 동등하게 표기하지 못하게 했다. 이격 극단의 0/5도 "실패"가 아니라 "12개월 선행"이라는 실측으로 기록해 다음 세션이 지우지 않게 했다. 레벨 문턱은 설계 그대로 유지 — 2건이 리프트 1.84x·정밀도 78%로 검증됐고 3건은 2011-07 한 달(국면 ① 최심점, MA 대비 −11.9%)만 켜져 희소한 극단 표시로 타당. **Phase 4** `data/manual_inputs/fx_risk_events.yaml`(semiconductor.yaml 동일 스키마) + `collectors/manual.py::fetch_fx_risk_events()` → **커버리지 90%→100%**. `engine/report/fx_regime_section.py` 신설, `payload.py`가 매일 빌드하고 `markdown.py` §1.5 뒤에 §1.7로 삽입 — **실제 파이프라인 실행으로 `report/2026-09.md` 140행 렌더 확인**. §1.7-B는 §6.5.3대로 사실 4줄(달러현금 기준선 3.94% / 환차익 구조 주의 / 종료경고 시 장기채 함의 / 위험보상 맥락)만 내고, 테스트가 금지 표현(헤지하·매수하·추천·비중을·목표 환율)을 렌더 결과에서 직접 잡는다 — 단 **부인 문구**("추천 아님", "목표 환율은 제시하지 않는다")는 위반이 아니라 정반대라 부정형을 구분(첫 작성 때 헤딩 "(판단 재료 — 추천 아님)"이 걸려 오탐). 개발 중 `series.py`가 미등록 지표(us_3m)에 **조용히 None을 주지 않고 KeyError를 낸 덕에** 누락이 즉시 잡혔다(설계 의도대로 작동). **Phase 5** `wiki/concepts/fx-regime-score-framework.md`(읽는 법 — 특히 "경고 4종은 같은 무게가 아니다" 리프트 표와 "FRS가 하지 않는 것" 표) + `wiki/monitoring/fx-regime-status.md`(Latest Status + append-only Check History) + `data/wiki_digest/fx-regime-score.yaml`(로더가 glob이라 자동 인식, 드리프트 검사도 자동). **자동화는 새 워크플로가 필요 없다** — `daily-peos-report.yml`이 이미 `engine.report.run`을 매일 돌리고 FRS가 그 payload 안에 있으며 네트워크도 안 쓴다(기존 수집 데이터만 읽음), 설계 §7의 "기존 일일 파이프라인 편입, 네트워크 미사용" 그대로. **최종**: 테스트 58건(series 7/regime 15/backtest 9/transition 13/section 14), 전체 회귀 384건 통과. 현 시점 출력 **FRS +29, 커버리지 100%, 원화 강세 국면, 전환 경고 0/4** — 유일한 강한 역풍은 미 10년물(−0.62, 4.80%)로 ⑤2021 국면을 끝낸 바로 그 요인이라 3개월 +50bp 임계 접근이 다음 체크포인트. **정리**: 로컬 파이프라인 실행이 남긴 산출물(report/·docs/·data/snapshots·peos_daily_history)은 egress 차단 상태의 열화 데이터인 데다 테스트가 감사추적 CSV에 픽스처(1999-02) 행을 또 남겨서 전부 되돌렸다 — 일일 워크플로가 정상 데이터로 재생성한다. 기존 실패 2건(test_rate_analysis, test_report_pipeline)은 무관함을 재확인(후자는 한국어로 개편된 리포트에 영어 헤딩 'Macro Dashboard'를 기대하는 낡은 테스트).


2026-09-10 18:20 KST — FIX(FRS §1.7을 HTML 리포트에도 연결 — "구현 완료" 보고의 구멍 정정) → 사용자 "구현을 완료한거야?" 질문에 상태를 확인하다가 **전달 채널에 섹션이 안 나오는 것을 발견**했다. Phase 4에서 §1.7을 `markdown.py`에만 연결하고 완료로 보고했는데, `daily-peos-report.yml`은 `report/<날짜>.html`을 이메일로 보내고 `report/YYYY-MM.html`을 `docs/report.html`로 게시한다 — **사용자가 매일 실제로 받아보는 건 HTML이고 마크다운은 저장소 안에만 있다.** `html_new.py`의 `fx_regime` 참조가 0건이었다. 이 저장소는 렌더러가 둘(markdown/HTML)인 구조라 새 섹션은 양쪽에 다 붙여야 전달된다 — 기존 plan에도 "HTML은 후속"으로 미뤄둔 항목이 있었는데 FRS엔 그 유예가 적용되면 안 됐다(설계 문서엔 "리포트 섹션: §1.7"이라고만 돼 있고 markdown 한정이 아니었다). **수정**: `html_new.py`에 `_render_fx_regime()` 추가(카드 형식 — 점수·8요인 표·경고 4종 표 + 실측 예측력 열·달러 자산 맥락 4줄), `render_html` 본문의 exposure 카드 뒤에 삽입. **계산은 하지 않는다** — `payload.py`가 한 번 계산한 `payload["fx_regime"]`을 markdown 쪽과 똑같이 읽어 표기만 한다(두 렌더러가 각자 계산하면 같은 날 리포트가 두 얘기를 하게 된다. `run.py` 88행에서 payload를 한 번 빌드해 103·104행이 각각 렌더하는 구조라 공유가 보장됨). R4 표기(포지션 지시 아님·추천 아님·목표 환율 제시 안 함)도 양쪽 동일하게 넣었다. **부수**: `html_new.py`에 HTML 이스케이프 헬퍼가 아예 없었다 — FX 근거 문자열엔 수동 입력(`fx_risk_events.yaml`)에서 온 값이 섞이므로 `_esc()`를 추가하고 적용. **테스트 5건 추가**(총 19건): HTML도 섹션을 낸다 / 두 렌더러가 같은 점수·라벨을 보고한다 / 데이터 없으면 양쪽이 함께 생략한다 / 수동 입력 문자열이 이스케이프된다(`<script>` 주입 검증) / R4 부인 문구가 HTML에도 있다. **교훈**: "구현 완료"를 렌더러 하나만 보고 판단하면 안 된다 — 전달 채널까지 확인해야 완료다.


2026-09-10 21:30 KST — FIX(미결 5건 일괄 해소 — 감사추적 오염·실패 테스트·뉴스 대조·레지스트리 중복·재시도·YoY) → 사용자 "특별히 문제 없다고 판단한 것 외에는 방법을 찾아봐". 직전 보고에서 "안 끝났다"고 남겨둔 항목을 전부 처리했다. **①감사추적 오염 차단**: 리포트 파이프라인 테스트가 `build_report_payload(month_key="1999-01")`로 돌면서 **저장소의 진짜 파일에 썼다** — `data/daily_signals/`에 `Recorded from PEOS pipeline (1999-01)` 같은 픽스처 행이 append됐고(하루 두 번 발생), 이 파일은 CLAUDE.md가 "append-only 감사추적, 절대 손대지 말 것"으로 지정한 것이라 되돌리는 것 말고 방법이 없다. `signal_recorder.py`가 Never Touch 목록이므로 **production 코드는 한 글자도 안 바꾸고** `tests/conftest.py`(신규)에서 쓰기 산출물 경로만 임시 디렉터리로 돌렸다(감사추적·일일이력·스냅샷·raw·`docs/reports-index.html`·`docs/archive/`). `data/normalized/`는 **읽기에도 쓰이므로 그대로 뒀다** — 돌리면 FRS 등 여러 테스트가 검증하는 대상 자체가 사라진다. 결과: 전체 스위트 실행 후 `git status`가 완전히 깨끗해졌다. **②실패 테스트 2건 해소 → 전체 그린**: `test_rate_analysis`는 진짜 `data/normalized/`에 2026-07-01 행을 심고 그게 최신이길 기대했는데 실수집이 2026-09까지 늘어 깨진 것 — 격리하니 두 번째 원인(심은 날짜가 71일 전이라 신선도 가드 45일에 걸림, production이 옳게 동작한 것)이 드러나 **오늘 기준 날짜 + 격리 디렉터리**로 시간 독립화. `test_report_pipeline`은 "Macro Dashboard"라는 옛 영문 헤딩에 묶여 있었고 5섹션 개편 때 "1. 거시 경제 대시보드"로 **이름만** 바뀐 것이라 단정을 현재 헤딩으로 교체. **그 과정에서 발견**: 이 테스트가 검증하는 건 `engine/report/html.py::render_html`인데 **매일 실제로 발송·게시되는 리포트를 만드는 건 `html_new.py`**다(run.py 34행) — production 렌더러엔 파이프라인 테스트가 없었다. `html.py::render_html`은 이 테스트에서만 호출되고 그 모듈이 살아있는 실질적 이유는 두 대시보드가 재사용하는 `_CSS`·`_esc` 뿐. 통합은 별건이라 **실제로 나가는 렌더러가 깨지지 않는지**를 고정하는 테스트를 추가했다. **③fx_risk_events.yaml 뉴스 대조**: WebSearch가 동작해 조사 가능했고, **제 초기 추정이 실제보다 낙관적이었다** — 9/9 브렌트 11월물 100.07달러, 이란-미국 무력충돌 격화, 후티의 사우디 에너지시설 공격, 호르무즈 우회 환적로 위협. 저장소 FRED 유가 계열은 $96에 머물러 있어 **수치만 보면 조용히 낙관을 유지**하는 상황이었다 — 뉴스 축이 존재하는 이유가 정확히 이것이고 이번에 실제로 그 역할을 했다. geopolitical_risk 0.55→0.80 상향 → 종료 트리거 '지정학 급등(2011형)' 발화, trigger_kind=NON_US_SHOCK. trade_policy_risk는 0.40 유지(미 반도체 관세 검토는 실재하나 한국 산업부가 "경쟁국 대비 불리하지 않은 대우" 확인, 전문가들도 실제 부과 가능성 낮게 봄 — 확정 사건 아님). **그리고 모델 결함 발견·수정**: 상향을 넣고 보니 중동 전쟁·유가 100달러인데 지정학 요인이 **+0.35(원화에 유리)** 로 읽혔다. 원인은 4개 축 **단순 평균** — "팬데믹 없음(0.00)"이 평균을 끌어내렸다. 팬데믹이 없는 건 정상 상태지 원화에 유리한 신호가 아니다. 역사도 최댓값 쪽(2011은 유럽위기 하나, 2020은 팬데믹 하나가 국면을 끝냄)이라 **최댓값 0.7 : 평균 0.3** 혼합으로 교체 → 지정학 +0.35→**−0.31(역풍)**, FRS +29→+22. 테스트 4건 추가(단일 극단이 희석되지 않는다 / 동시다발이 단일보다 나쁘다 / 축이 없는 것과 0인 것은 다르다(R3) / 상세에 최고치 표기). **④FRED 레지스트리 두 벌 재발 방지**: 통합은 위험해서 미루되, `build_api_catalog.py`가 `collectors/fred.py`만 읽던 것을 **`macro_data.py` PRESETS도 함께 읽도록** 고쳤다. 표에 "정의 위치 → 저장 위치" 열을 추가해 `us_10y`(DGS10, macro-series)와 `us_10y_treasury`(DGS10, normalized)가 같은 series_id를 가리킨다는 게 한눈에 드러난다 — 오늘 금리 4종을 이미 있는데 또 백필한 게 카탈로그에 안 보였던 탓이라 같은 실수가 반복되지 않게 했다. **⑤core10-collect 재시도**: 다른 수집 워크플로는 전부 `until` 재시도가 있는데 여기만 없었다. 그런데 같은 형태를 못 쓴다 — `collect_core10`은 `--strict` 없이는 항상 exit 0이라 `until`이 한 번도 안 돌고, `--strict`를 붙이면 **구조적으로 못 가져오는 계열** 때문에 매일 실패해 6시간을 헛되이 태운다. 그래서 exit code가 아니라 **성과("N/M succeeded")가 나아지는 동안만** 재시도하게 했다. 시뮬레이션 검증: 7/10→9/10→10/10은 3회 시도 후 완료, 8/10→8/10은 2회 만에 "구조적 결손으로 보고 중단"(6시간 낭비 없음). **⑥k_employed_yoy 해소**: 2026-09-08에 "표는 연결되지만 레벨값(2,900만 명)만 줘서 좌표만 바꾸면 CCI K-Sahm이 그걸 증감률로 받는 조용한 오작동"이라며 보류했던 건. `collectors/kosis.py`에 `_to_year_over_year()`를 추가하고 spec에 `derive: "yoy"`를 선언하는 방식으로 해소 — **선언한 지표만 변환**되므로 이름만 `_yoy`인 지표가 레벨을 흘리는 사고는 재발하지 않는다. 12개월 전 값은 **날짜로 직접 찾는다**(인덱스 −12가 아니라 — 월 결측 구간에서 엉뚱한 달과 비교하는데 값이 그럴듯해 눈에 안 띔), 짝 없는 달은 0%가 아니라 버린다(R3), `unit`도 `Persons`→`%`(안 바꾸면 리포트가 "2,900만 %"를 찍는다). 기존 `kosis_k_employed_yoy.csv`가 아예 없어 레벨/YoY 혼입 위험도 없음을 확인. 테스트 7건. **최종**: 전체 회귀 **403건 통과, 실패 0건**(세션 시작 시점엔 2건 실패). 카탈로그 드리프트 검출기가 ⑥의 변경을 자동으로 잡아내 재생성한 것도 설계대로 동작한 사례.


2026-09-10 23:50 KST — REFACTOR(engine/report/html.py 정리 — 죽은 렌더러 삭제 + 누락 섹션 6종 이식) → 사용자 "html.py 정리도 진행해줘". **조사 결과 단순 죽은 코드가 아니었다**: `html.py::render_html`은 아무 데서도 호출되지 않지만(`run.py`는 `html_new.py`를 쓴다), 그 안에 **매일 발송되는 HTML 리포트가 통째로 빠뜨린 섹션들의 구현**이 들어 있었다. 실측 대조(마크다운 vs HTML): Executive Summary 1→**0**, Action Plan 11→**0**, 시나리오 4→**0**, 논의사항 1→**0**, 경제캘린더 2→**0**, 그리고 **거시 대시보드 자체가 없어** 실질 GDP·실업률이 HTML에 0건이었다. 즉 "구현은 있는데 전달은 안 되는" 상태로 방치돼 있었다. 사용자 선택(AskUserQuestion)에 따라 **이식 후 삭제**로 진행. **①공용 분리**: `_CSS`(7,482자)·`_esc`만 살아 있었고(대시보드 2종이 import) 나머지는 전부 죽은 코드였다. `engine/report/html_shared.py`(신규)로 옮기고 바이트 단위 동일성 확인 — 살아 있는 대시보드가 **쓰이지 않는 렌더러에 의존**하던 얽힘을 끊었다. `daily_dashboard`·`real_estate_dashboard` 실제 렌더로 검증(423KB·24KB, CSS 포함, DOCTYPE 정상). **②섹션 7종 이식**: Executive Summary·Action Plan(우선순위 5단계 카드)·시나리오 분석(Base/Bull/Bear + 깨지는 조건)·경제 캘린더·논의사항(답변 입력+복사/이슈제출 JS 90줄 포함)·Personal Executive Brief·**Macro Dashboard(10지표 표 + 10년 추세 스파크라인 SVG)**. **마크업을 복사하지 않았다** — `html.py`는 밝은 테마(`--surface` CSS 변수)에 `<section class="card">`를 쓰고 `html_new.py`는 어두운 그라데이션에 자체 인라인 CSS를 써서 클래스가 서로 없다. 내용만 가져오고 표현은 이 파일 양식으로 다시 썼다(스파크라인 색도 `var(--accent)`→고정색). † ‡ 각주("이월값" vs "전월 스냅샷 없어 원자료 이력 사용")는 그대로 옮겼다 — 표기가 사라지면 이월값이 실시간값처럼 보이기 때문(R2·R3의 실무적 표현). 경제 캘린더는 이벤트가 없으면 "확정된 일정 없음"을 매일 찍는 대신 **섹션 자체를 생략**하게 바꿨다(없는 걸 매일 찍으면 그것도 신호처럼 읽힌다, R3). **③삭제·이관**: `html.py`(717줄) 삭제, `tests/test_report_pipeline.py`의 `render_html` import를 production 렌더러로 교체하고 단정도 현행화(`<!doctype html>`→`<!DOCTYPE html>`, `<section>` 짝→`<div>` 짝). 이 테스트가 죽은 렌더러만 검증하고 있어서 **"검증된 렌더러"처럼 보였던** 게 이번 정리의 출발점이었다. 위키 `automation-vs-ai-narrative-roadmap.md`의 `html.py` 참조 2건도 갱신. **④실물 검증**: 파이프라인 실행 → `report/2026-09-10.html` 생성 후 이전 커밋본과 대조 — 이식한 8개 항목이 전부 0→1건 이상, 기존 섹션(포지션·위기지수·금리·FX·실거래) 전부 유지, 크기 17,962→98,838바이트(5.5배), div 짝 160/160, 스파크라인 17개, 피드백 JS 포함. Playwright 스크린샷으로 다크 테마에서 실제로 그려지는 것까지 눈으로 확인(가로 스크롤 넘침 없음). 전체 회귀 403건 통과. 로컬 실행 산출물은 egress 차단 상태의 열화 데이터라 커밋하지 않음.

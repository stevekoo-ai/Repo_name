---
title: Log Archive 2026-09-07
created: 2026-09-07
updated: 2026-09-07
tags: [log, archive]
---

# Log Archive — 2026-09-07

아카이브: `wiki/log.md`에서 로테이션(3인 하이브리드, GitHub Actions 층)이
이관한 2026-09-07 로그 항목. 원본은 append-only 그대로 보존.

## 당일 log

2026-09-07 — FEAT(SK하이닉스 120일선(MA120) 추세 추적 자동화 — 위키 판독법 신설 + PEOS/SK하이닉스 리포트 코드 반영) → 사용자가 대화에서 반복 요청한 세 가지 분석("120일선 최근 기울기로 전고점까지 며칠", "화면 07-02~오늘 구간 눈대중과 대조", "8/20~9/7 추적 품질 검토")을 손 계산 대신 "일단 이 그래프 판독법에 대해서 위키에 저장하고 보고서에 추가해서 트레이스해줘" 요청으로 정식 코드화. **구현**: `scripts/ma120_trend.py`(신규, hbm_cycle_score.py/capex_periphery.py와 동일한 공유모듈 패턴 — daily_report.py 사이블링 임포트 + engine.report.payload.py 패키지 임포트 양쪽 다 되는 try/except) — `sources/daily-price-history.csv`(2023~, 긴 이력)와 `sources/sk-hynix-price-snapshot.csv`(최신)를 합쳐 주말 캐리포워드만 제외한 거래일 시계열을 만들고, MA120을 계산 → 최근 20거래일 고정창으로 선형회귀 기울기를 구해 250일 최고가(day250_high, 이미 수집되는 필드, 새로 계산 안 함) 도달 예상 거래일/달력일/날짜를 선형 외삽 + 같은 20거래일 창에서 종가가 MA120 위/아래로 며칠씩 있었는지·교차 횟수·평균/최대/최소 괴리율을 집계(추적 품질). 120거래일 미만 이력이면 창작 없이 data_status="pending" 반환(R3). **왜 20거래일 고정창인가**: 대화 중 5/10/20/30일 창을 다 계산해봤는데 20일이 가장 안정적(원자료 종가 기준으론 10일 창이 부호까지 뒤집혔음) — 자동화는 매번 사람이 구간을 고를 수 없어 이 값을 기본값으로 고정, 특정 이벤트 기준 구간(07-02 등)은 자동 계산과 별개로 사람이 그때그때 재확인하도록 위키에 명시. **반영 위치**: `engine/report/payload.py`(payload["ma120_trend"], try/except 방어, R4 무관 — 순수 가격 기하학) → `engine/report/markdown.py::_sk_hynix_decision_section()`에 신규 서브섹션(HBM Cycle Score 자동채점 블록 바로 뒤) → `scripts/daily_report.py`(SK하이닉스 3x/일 리포트)에도 같은 모듈로 압축판 추가. **테스트**: `tests/test_ma120_trend.py`(신규 7개) — 완벽한 선형 합성 시계열로 기울기·ETA·추적통계 기대값을 손으로 정확히 검증(선형 시계열의 MA120도 같은 기울기로 선형이 된다는 성질 이용), 이력 부족 시 pending, 기울기 0 이하 시 억지 ETA 안 만듦, 이미 전고점 도달 시 0일 처리, 실제 저장소 데이터 스모크 테스트. **위키**: `wiki/concepts/ma120-trend-tracking.md`(신규) — 판독법 자체(왜 20거래일인지, 추적 품질 읽는 법, 한계) + 이번 대화의 실전 사례 2건(07-02~오늘 46거래일 창이 자동계산 20거래일 창보다 사용자 눈대중과 더 정확히 일치했던 사례, 8/20~9/7 추적에서 "꾸준한 상승"이 아니라 "9/2~9/4 이탈→9/7 급등 재돌파"였다는 검증 결과)을 그대로 기록. **부수 발견 및 승격**: 8/19 발표일 120일선 이탈(150.0만원 < MA120 162.6만원)→8/20 매수 첫날 재돌파(169.1만원 > MA120 163.1만원)가 실측으로 확인돼, 기존 체크포인트⑤-1의 "사용자 메모 기반 [미검증]" 기술적 신호를 [FACT]로 승격(단 메모의 "저점 116.8만원" 수치 자체는 실측 저점 142.0~142.2만원대와 달라 여전히 [미검증]으로 구분 유지). **버그 수정(같은 세션)**: 직전 "$1조 클럽" INGEST 때 `wiki/concepts/sk-hynix-analyst-thesis-checkpoints.md`의 `updated:` frontmatter만 갱신하고 `data/wiki_digest/sk-hynix-analyst-thesis-checkpoints.yaml`의 `as_of`를 안 고쳐서 `test_wiki_digests_load_from_the_real_repo_and_none_are_stale`가 깨져 있던 걸 발견·수정(digest as_of를 09-04로 갱신 + 요약 내용 교체). 전체 회귀 `PYTHONPATH=. python tests/test_exposure_reconciliation.py` 87/87 통과, `pytest tests/` 234/236 통과(동일한 무관 pre-existing 실패 2개 제외). 실측 렌더링으로 PEOS·SK하이닉스 리포트 양쪽 다 크래시 없이 확인.

2026-09-07 — ANALYSIS(원·달러·엔·위안 4개국 통화 상호 영향 분석 — 관측시각 함정 발견) → 사용자 요청 "10년을 베이스로 1년 상호 영향성 진단, 최근 한 달로 트렌드 확정, 앞으로 한 달 전망. github API로 효과적으로 수집". **데이터 갭**: 저장소에 원/달러(ECOS 10년치)만 있고 엔·위안이 아예 없었음 + 이 세션은 FRED 직접 접속이 프록시 차단이라 GitHub Actions 경유가 유일한 경로 → `scripts/macro_data.py` PRESETS에 usd_jpy(DEXJPUS)·usd_cny(DEXCHUS)·usd_krw_fred(DEXKOUS, 교차검증용) 추가 후 main 반영 → `macro-data-sync.yml`을 GitHub API로 트리거해 10년치(3,652일) 백필 수집(4,982행 확보). **핵심 발견 — 관측시각 함정**: ECOS(서울마감 15:30 KST)와 FRED(뉴욕정오 12:00 ET)를 날짜 라벨만 맞춰 상관분석하면 KRW–JPY +0.049, KRW–CNY −0.063으로 "원화는 엔·위안과 무관"이라는 완전히 틀린 결론이 나옴. 교차검증용으로 같이 받아둔 DEXKOUS로 검증 — **같은 원/달러 환율인데 ECOS vs FRED 동시상관 +0.058**(진짜 상관은 1.0에 가까워야 함), 1일 시차 주면 +0.448 → 날짜 라벨이 같아도 다른 시점을 찍고 있음이 확정. 관측시각 통일(FRED 3종) 후 실측은 KRW–JPY +0.475, KRW–CNY +0.311. 부수적으로 불일치 상태에서 나오던 "CNY가 KRW를 1일 선행(+0.449)"도 가짜였음 — 통일 후 세 쌍 전부 lag=0 동시. **구조 변화**: 10년 평균은 KRW–CNY(+0.400)>KRW–JPY(+0.244)였는데 최근 1년 역전(+0.475 vs +0.311), 최근 1개월 격차 확대(+0.639 vs +0.402), 63일 롤링에서 KRW–CNY는 0.537→0.097로 사실상 단절 — 원화 동조 대상이 위안에서 엔으로 이동. 위안 연율변동성은 10년 4.03%→1년 2.04%→1개월 0.98%로 억제 심화(원인 규명은 [미검증]). **최근 1개월 트렌드**: KRW −3.99%(강한 원화 강세, ECOS 최신 09-07 1,355.20원, 12주로는 −11.0%), JPY +0.51%(160엔 벽 정체), CNY −0.37%(사실상 고정). **전망**: 사용자가 "통계적 밴드+시나리오" 수준을 택해 최근1년 일간σ×√21 밴드만 제시하고 확률·단일목표치는 만들지 않음(R3). **반영**: `wiki/concepts/fx-cross-currency-krw-usd-jpy-cny.md`(신규, 함정→실측→판독규칙 5개 순서로 구성) + `wiki/index.md`. 사용자 선택에 따라 리포트 자동화는 이번 범위 제외(채팅+위키까지만).

2026-09-07 — FIX+FEAT(API 전수 복구 + 데이터 카탈로그 신설) → 사용자 요청 "fred든 뭐든 API를 다 찔러봐" → "API관련 복구는 데이터 수집 자동화를 위해 매우 필요한 조치다. 다 정상 동작하도록하고, 경제관련 보고서 중 먹을 수 있는 데이터가 있다면 적극적으로 사용하자. 각 API가 가져올 수 있는 데이터의 종류를 확인하여 위키에 저장하고 lookup table을 만들어보자". **발단**: `scripts/api_probe.py`+`api-probe.yml`(신규, workflow_dispatch 전용, 16개 소스에 최소 1회씩 호출해 ALIVE/EMPTY/DEAD/SKIPPED 4단계 보고 — "키 미설정"과 "API 장애"를 섞지 않는 것이 핵심)를 만들어 돌린 결과 여러 장애가 한꺼번에 드러남. **최대 사고 — 부동산 실거래 수집이 한 달간 조용히 정지(2026-08-11~09-07)**: `real-estate-sync.yml`은 234회 연속 "success"를 찍고 매일 커밋했지만 실제 데이터 파일은 0건, `automation-run-log.csv`와 `reports-index.html`만 갱신되고 있었다. 원인이 **4중으로 겹쳐** 있어 하나 벗길 때마다 다음 게 드러남 — (1) **XML 파싱**: 서버는 resultCode 000/OK에 진짜 거래 데이터를 정상 응답하는데 `type=json`을 요청해도 XML로 온다. 코드는 `resp.json()`만 부르다 ValueError를 냈고 그 실패를 "likely a service/auth error"로 **오진**해 몇 달간 "data.go.kr 승인 문제"로 잘못 기록돼 있었다 → `collectors/base.parse_data_go_kr_items()` 신설(형식을 신뢰하지 않고 JSON/XML 양쪽 파싱, 성공/실패는 형식이 아니라 resultCode로 판정, 게이트웨이 거절 봉투 `OpenAPI_ServiceResponse/cmmMsgHeader/errMsg`는 별도 식별해 "활용신청 미승인" 진단 유지 — 기존 테스트 3건의 의도는 옳았으므로 근거만 추측→실측 식별로 교체), MOLIT 4종이 이 공유 파서를 쓰도록 통일. HTML 오류 페이지가 ElementTree 기준 "멀쩡한 XML"이라 거래 0건으로 오독되는 경로도 차단. (2) **connect 타임아웃 8초**: 파싱 수정 후에도 4종 전부 connect timeout — 같은 러너에서 몇 분 전 프로브는 timeout=20으로 4종 다 성공(강남구 202608: 매매 64건/전월세 941건/빌라 61건/오피스텔 44건)했으므로 막힌 게 아니라 8초가 짧았던 것 → `(15, 30)` 튜플로 변경. (3) **단일 지역 서킷브레이커**: `all_regions[0]`(종로구) 한 곳만 찔러보고 실패하면 55개 지역을 통째로 포기 → `kr_regions.probe_regions()` 신설(목록 전체에 분산된 카나리아 3곳이 **전부** 실패할 때만 소스 사망 판정, 진짜 죽었으면 여전히 몇 초 안에 끝나므로 서킷브레이커 목적은 유지). (4) **커밋 단계 잠복 버그**: `git add data/normalized/molit* sources/molit*.csv`에서 후자가 존재하지 않아 "pathspec did not match"로 **add 전체가 실패**했고 `|| true`가 은폐 → 스테이징 안 된 변경 때문에 rebase가 "cannot rebase: You have unstaged changes"로 사망. 수집이 0건이던 동안엔 스테이징할 게 없어 안 보였다 — **장애가 장애를 가리고 있었다**. 경로별 분리 add + `rebase --autostash`(5개 워크플로 전체 적용). **결과 실측**: 147개 파일에 2026-08 데이터 유입, 서울 아파트 매매 평당 41,322,500원(7월 대비 −6.5%)·거래량 2,589건(−41%, 단 8월말 계약분 신고기한 미도래로 과소집계), 전세 25,608,872원(−1.2%), 대시보드도 8월 반영. **BLS 신설**: `BLS_API_KEY`가 daily/monthly-peos-report.yml 두 워크플로에 주입되는데 저장소 전체에 이를 쓰는 코드가 0줄이었음 → `collectors/bls.py` 신설. FRED와 겹치는 축을 대체하지 않고 **FRED가 안 주는 축**만 채움(시간당 평균임금 CES0500000003, JOLTS 구인건수, 경제활동참가율) — 이 저장소 판단 사슬의 맨 앞단(미국 고용·임금→연준→미10년물→원/달러→하이닉스 수급 / 미국금리→한국기준금리→주담대)이 CPI·실업률 두 개로만 커버되고 있었기 때문. v2는 키 없이도 동작(일 25회). **BLS 2차 조용한 실패**: 첫 수집이 8개 시리즈 전부 2016-01~2025-12(정확히 120개월)에서 멈춤 — v2의 연도 범위 한도는 **포함 연수**로 세는데 `startyear=올해-10`이면 11년 요청이 되고 BLS는 에러 없이(status=REQUEST_SUCCEEDED) 앞 10년만 주고 최신 연도를 버린다 → `_HISTORY_YEARS` 9로 수정, 재수집 후 2026-08까지 복구(임금 $37.75, 실업률 4.1%, JOLTS 7,271천건). **IMF 신설**: `collectors/imf.py` — 이 저장소에서 **유일하게 미래 값(2031년까지 전망)을 주는 소스**. 실측(`imf_*`)과 전망(`imf_*_forecast`)을 반드시 분리 저장하고 DataPoint는 언제나 실측만 반환(전망을 value로 주면 호출부가 현재값으로 쓴다). 경계선은 API가 안 알려주므로 보수적으로 작년까지 실측·올해부터 전망. KOR/USA/CHN/JPN — 같은 날 4개국 통화 분석과 같은 비교군. OECD SDMX는 살아있음을 확인했으나 FRED가 이미 미러를 제공해 중복이라 미구현으로 남김. **KOSIS 진단 정정(2회)**: 프로브에서 데이터 엔드포인트가 "해당 통계표가 존재하지 않습니다"로 **응답**(서버·인증 정상, 좌표만 오류)하는 걸 보고 "네트워크가 아니라 ID 오류"라 단정했으나, 검색 엔드포인트를 실제로 돌리니 4회 연속 connect timeout=20 → **문제가 두 개**임이 확정(좌표 오류 + kosis.kr 연결 불안정). 기존 주석("TCP 타임아웃/한국 IP 제한, 재시도 무용")은 연결 문제에 대해선 맞는 관찰이었으나 그게 시리즈 실패 원인이 아닌데 하나로 뭉뚱그려져 "손쓸 수 없다"로 결론나 좌표를 아무도 고치지 않은 채 몇 달이 감 — 두 문제를 분리해 재기술. `kosis_lookup.py --search`(statisticsSearch.do, 기존 주석의 "KOSIS엔 검색 API가 없다"는 단언이 틀렸음) 추가했으나 해당 엔드포인트는 Actions에서 접근 불가로 확인, 좌표 확정은 미완(접근 가능한 데이터 엔드포인트로 후보 검증하는 기존 모드를 써야 함). **🔐 보안 사고 및 조치**: KOSIS 재시도 로깅을 `str(exc)[:160]`으로 자르며 requests 예외에 박힌 `?apiKey=...`가 **잘린 채** 공개 Actions 로그(이 저장소는 public)에 노출됨 — GitHub 시크릿 마스킹은 **값 전체 일치 시에만** 동작하므로 **자르는 행위 자체가 마스킹을 무력화**한다. 노출 run 로그 삭제 + `kosis_lookup.py`/`api_probe.py` 전부 자르기 **전에** `redact_url` 경유하도록 수정 + 회귀 테스트 3건(`str(exc)[:`/`resp.text[:` 패턴 재발 차단). **KOSIS_API_KEY 재발급 필요(사용자 조치 대기)**. **부수 발견**: `ecos-lookup`/`kosis-lookup`/`molit-diagnostic` 3개 진단 워크플로가 `pip install requests`만 해서 `ModuleNotFoundError: pandas`로 죽고 있었음(collectors.base가 pandas, core.config가 pyyaml 사용) — 진단 도구 자체가 조용히 못 돌고 있었다. **lookup table(사용자 명시 요청)**: `wiki/concepts/api-data-catalog.md` — **손으로 쓰지 않고 `scripts/build_api_catalog.py`가 각 수집기의 시리즈 정의를 직접 읽어 생성**(단일 출처는 코드). 이유: 같은 날 "문서·주석이 코드와 어긋나 오래 헤매는" 사고를 두 번 겪음(kosis.py 실패 원인 오진, kosis_lookup.py의 검색 API 부재 단언). 9개 소스의 지표 key/코드/주기/단위 + 소스별 실측 함정(FRED 환율 10일 지연, 국토부 type=json인데 XML, BLS period M13은 연평균 및 연도범위 포함연수, 관세청 1년 초과 거부, KIS 토큰 재발급 403) + **아직 못 가져오는 것** 목록(그게 없으면 "없는 데이터"와 "있는데 안 쓰는 데이터"를 구분 못 하고, BLS가 정확히 후자였다). `tests/test_api_catalog.py` 5건이 문서-코드 드리프트·시리즈 누락·index.md 등록(고아 페이지 방지)을 CI에서 강제 — 실제로 index.md 등록 누락을 이 테스트가 잡음. **리포트 반영(1.5절)**: `engine/report/us_labor_outlook.py`+markdown 1.5절 신설 — BLS 4지표 전월대비 + 4개국 실질GDP성장률 실측 vs IMF 전망. 네트워크 미사용(정규화 CSV만 읽음), 미수집 지표는 생략(0으로 렌더하면 "임금 0달러"로 읽힘, R3), 전망은 "예측이지 실측이 아니다" 명시(R1), 매매 지시 아님을 문구로 강제(R4), 한쪽 소스만 있어도 그쪽은 렌더(한 소스 장애가 멀쩡한 다른 소스를 가리지 않도록). `us-labor-outlook-sync.yml`(주 1회 월 07:00 KST — BLS 월간 지표라 매일 돌릴 이유 없고 키리스 한도만 태움). 전체 테스트 284건 통과(무관 pre-existing 실패 2건 제외).

---
title: Log Archive 2026-09-08
created: 2026-09-08
updated: 2026-09-08
tags: [log, archive]
---

# Log Archive — 2026-09-08

아카이브: `wiki/log.md`에서 로테이션(3인 하이브리드, GitHub Actions 층)이
이관한 2026-09-08 로그 항목. 원본은 append-only 그대로 보존.

## 당일 log

2026-09-08 06:00 KST — CODE+INGEST(KOSIS 공식 카탈로그 도구 실측 + wiki/concepts/kosis-category-catalog.md 신설) → 이전 세션에서 신설한 `scripts/kosis_catalog.py`(통계목록·통계표설명 API)를 공식 매뉴얼(`docs/kosis/openApi_manual_v1.0.pdf`, GitHub Actions로 다운로드 완료)과 대조해 필드명·파라미터 버그 2건 수정: ①`table_item_metadata()`의 출력 필드가 실제로는 `OBJ_VAL_ID`/`OBJ_VAL_NM`이 아니라 `ITM_ID`/`ITM_NM`(자료코드)·`OBJ_ID`/`OBJ_NM`(분류)·`UP_ITM_ID`(상위코드) — 매뉴얼 §2.5.3.4 확인 후 정정 ②`list-category`의 `--parent-list-id` 기본값을 매뉴얼 예제(parentListId='A', "최상위 목록 생성" 주석)를 보고 'A'로 잡았다가, 실제 워크플로 실행 결과 'A'가 root가 아니라 "인구" 대분류 자체임을 확인 → 빈 문자열로 되돌림(실측: 진짜 최상위는 30개 대분류 A~V). `kosis-catalog.yml` 워크플로를 8회 실행(2회는 KOSIS 간헐적 연결 두절로 실패, 재시도로 성공) — 최상위 30개 대분류 전체 확보, 광업ㆍ제조업(L)→광업제조업동향조사(L_4)→생산·출하·재고(101_G131) 드릴다운으로 통계표 11개 발견(DT_1F02001·DT_1F02012 등), DT_1F02012 품목별 item-meta로 72개 품목 확인했으나 반도체 없음(석유화학/철강/자동차부품/전기가스 위주) — industrial_production_index·semiconductor_shipment/inventory_index 미해결 지속, 다음은 DT_1F02001(산업별) item-meta 시도 예정. 노동(D) 드릴다운은 2회 연결 실패로 미완료. **중요 발견**: `.gitignore`의 `data/raw/*/*.json`이 저장소 전체에 걸친 기존 정책이라 `kosis-catalog.yml`의 raw JSON 커밋 스텝이 매번 조용히 no-op됐음(매번 "No new catalog artifacts") — raw-tier는 원래도 버전관리 대상이 아니므로 이건 이 저장소 관례를 따른 것이지만, 사용자가 명시적으로 요청한 "확보해서 저장"을 만족시키려면 실측 결과를 재사용 가능한 문서로 남겨야 함을 깨닫고 방향 전환. **반영**: `wiki/concepts/kosis-category-catalog.md`(신규) — 최상위 30개 대분류 표, L 드릴다운 경로와 통계표 11개 표, DT_1F02012 품목 확인 결과, 다음 시도할 것 명시. `scripts/kosis_catalog.py` docstring에 raw JSON 비커밋 사실과 이 wiki 페이지로의 링크 추가. `tests/test_kosis_catalog.py` 3건 추가(objId/itmId 선택 전달, list-category 기본 root 값 통합테스트). `wiki/index.md`에 신규 항목 추가. 전체 회귀 `pytest tests/` 통과(pre-existing 무관 실패 2건 제외, `test_rate_analysis.py`·`test_report_pipeline.py` — main에서도 동일하게 실패함을 확인). claude/ai-agent-impl-002tip → main 양방향 동기화 완료.

2026-09-08 06:10 KST — CODE(KOSIS 좌표 확정 완료 — 7개 중 6개, DT_1F02001 하나로 industrial_production_index·반도체 출하/재고 3개 동시 해결) → 직전 항목에서 발견한 DT_1F02001(시도/산업별 광공업생산지수)을 item-meta로 계속 파고들어 산업별(objL2) 분류에 **C261=반도체 제조업**이 있음을 확인 — 같은 표의 ITEM 분류(T10=생산지수/T11=출하지수/T12=재고지수)와 조합하면 미해결 3개 지표를 표 하나로 전부 커버할 수 있다는 걸 발견. 하지만 "분류표에 코드가 존재한다"와 "그 조합에 실제 데이터가 있다"는 별개(과거 industrial_production_index 실패가 정확히 이 함정)라 `scripts/kosis_catalog.py`에 `verify-data` 서브커맨드(statisticsParameterData.do 직접 호출)를 신설해 3개 조합 전부 실데이터로 확인: T10/00/0(총지수)=2026-07 120.4, T11/00/C261=2026-07 159.1(반도체 출하), T12/00/C261=2026-07 106.5(반도체 재고). 연결 두절로 1~2회씩 재시도가 필요했으나(이미 문서화된 현상) 최종적으로 전부 확정. **반영**: `collectors/kosis.py`에 `obj_l2` 선택 파라미터 지원 추가(`_fetch_table`), `industrial_production_index`·`semiconductor_shipment_index`·`semiconductor_inventory_index` 3개 좌표를 DT_1F02001 기반으로 교체(industrial_production_index는 "광공업"이지 "전산업"이 아니라는 스코프 주의사항 명시 — retail_sales_index 때와 같은 종류의 함정). k_employed_yoy만 YoY 계산 로직 부재로 미해결 유지. `tests/test_kosis_lookup.py`에 회귀 고정 테스트 4건 추가(3개 좌표 드리프트 방지 + `_fetch_table`의 objL2 포함/생략 케이스). `scripts/build_api_catalog.py` LIVE_STATUS·결과표·"아직 못 가져오는 것" 섹션을 6/7 확정 상태로 갱신 → `wiki/concepts/api-data-catalog.md` 재생성(`--check` 통과). `wiki/concepts/kosis-category-catalog.md`의 "다음 시도할 것"을 확정 결과 표로 교체. 전체 회귀 `pytest tests/` 통과(동일한 무관 pre-existing 실패 2건 제외). claude/ai-agent-impl-002tip → main 양방향 동기화 예정.

2026-09-08 06:20 KST — CODE(반도체 출하/재고 KOSIS 수집 진입점을 score_semiconductor_cycle()에 직접 추가) → 직전 항목에서 좌표는 확정했지만(DT_1F02001) fetch_series()를 실제로 호출하는 곳이 없어 CCI 반도체 사이클 모듈이 계속 US industrial production 폴백만 쓴다는 걸 발견 — 어디에 수집 진입점을 넣을지 사용자에게 3안(스코어링 함수 내 직접 호출/collect_core10.py 확장/신규 스크립트) 물어봄. 사용자가 "score_semiconductor_cycle()에서 직접 fetch_series 호출"(추천안)을 선택. `engine/crisis_analysis/scoring.py`의 `score_k_sahm()`이 이미 쓰고 있던 정확히 같은 패턴(스코어링 함수 안에서 계산 직전 `kosis.fetch_series()` 직접 호출 → 스스로 정규화 CSV를 채움)을 `score_semiconductor_cycle()`에도 적용 — 새 스크립트·워크플로 없이 CCI 계산 시점(리포트 생성마다)마다 자동 갱신되고, 실패해도 기존 US 폴백 경로가 그대로 안전망. `tests/test_cci_fallback.py`에 테스트 2건 추가(fetch_series가 두 시리즈 키 모두 호출되는지, 실데이터가 있을 때 KOSIS 경로로 cycle_index를 정확히 계산하는지 — 출하/재고 변화율 차이 공식 검증). `collectors/kosis.py` 모듈 docstring의 "후속 과제" 메모를 해소 완료로 갱신. 전체 회귀 `pytest tests/` 통과(동일 무관 pre-existing 실패 2건 제외). claude/ai-agent-impl-002tip → main 병합·푸시 완료.

2026-09-08 06:25 KST — INGEST(케인즈학파 vs 시카고학파 — 일반 거시경제학 이론 배경지식 신설) → 사용자 질문 "케인지와 시카고 학파의 차이점이 뭐냐?"에 Claude가 일반 지식으로 답변 후, 사용자가 "정리해놔바"로 위키 반영 요청. `wiki/index.md` grep 결과 이 저장소에 거시경제학파 비교를 다루는 기존 페이지 없음(fundamentals-vs-sentiment-derating.md에 케인즈 격언 인용 1건뿐, 주제 겹침 아님) → 신규 페이지로 판단. **반영**: `sources/general-econ-keynesian-vs-chicago-school-2026-09-08.md`(신규, Claude 답변 원문 [OPINION/일반지식] 태깅) → `wiki/concepts/keynesian-vs-chicago-school.md`(신규) — 시장 자기조정 능력·정부 역할·통화 vs 재정정책·합리적 기대·대공황 해석 5개 축 비교 + 한 줄 요약. PEOS 엔진이 직접 참조하는 프레임워크는 아니며 economic-events-framework.md(거시 이벤트) 등에 이론적 배경으로만 교차 참고 가능하다고 명시. `wiki/index.md`에 신규 항목 추가. 코드 변경 없음(순수 위키 페이지 신설).


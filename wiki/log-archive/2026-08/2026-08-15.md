---
title: Log Archive 2026-08-15
created: 2026-08-15
updated: 2026-08-15
tags: [log, archive]
---

# Log Archive — 2026-08-15

아카이브: `wiki/log.md`에서 로테이션(3인 하이브리드, GitHub Actions 층)이
이관한 2026-08-15 로그 항목. 원본은 append-only 그대로 보존.

## 당일 log

2026-08-15 — RESTRUCTURE(트랙 A 재구성 완료 + main 반영 + CXL 브랜치 격리 + API 재사용성 검토) → 사용자 "경제부분을 옮겨서 재구성하는 작업을 시작하자 / CXL 브랜치들은 회사 LLM이 연동해 동작해야 하니 독립적으로 동작하도록 격리해줘 / API들을 객체화해서 재사용 검토해줘 / github 스케줄러로 데이터를 계속 쌓아가게 해줘". **① 브랜치 재구성 완료**: `origin/main`(08-11 보안 퍼지, 74커밋, 공통조상 없음)에서 `claude/ai-agent-impl-002tip`을 다시 만들고, 이전 424커밋 이력(로컬 백업 태그 `backup/pre-reorg-002tip-20260814`)의 파일을 하나씩 main과 대조해 이식 — 블라인드 복사가 아니라 매 파일마다 최신 판정: bot 유지 데이터(sources/*.csv, data/normalized/*, config/portfolio.yaml, subscription_monitor 상태)는 main이 이미 더 최신이라 **main 그대로 유지**(예: 보유종목 main 08-14 KIS 실시간 vs 이 브랜치 08-12), 이 세션에서 검증된 코드 수정(ECOS 페이지네이션, 렌더러, reconciliation, exposure model 등)만 이식. 부수 발견 2건: (a) 여러 wiki 파일의 거대해 보이던 diff가 실은 CRLF 오염이었음(정규화 후 실제 delta 확인) (b) wiki/log.md의 "당일 log 중복 섹션"은 사전 존재 버그이지 콘텐츠 유실이 아님(main의 모든 항목이 이 브랜치 안 어딘가에 실존함을 문자열 대조로 확인 후 이식). CLAUDE.md의 "GitHub push+email 전면금지"(08-11 작성, root+wiki 양쪽)를 오늘 지시대로 트랙 B 전용으로 좁힘. `upload_wiki_files.py`(회사망 우회 도구, "범용" 표방이나 패턴은 dispatch류와 동일)는 **이식 제외**. 파이프라인 재실행으로 main의 최신 데이터 기준 리포트 재생성(반도체 96.1%·3.56억·조정가능 181주는 유지, 보유수량은 KIS 최신 반영) — 재생성 과정에서 `daily-peos-report.yml`의 Pages 발행 스텝 버그 발견·수정: `report/*.html` 글롭이 `subscription-desktop-*.html`도 포함해 정렬상 최신 월간 리포트보다 뒤에 와 `docs/report.html`에 **엉뚱한 리포트가 발행될 뻔함** — 패턴을 `<YYYY-MM>.html` 명시 매칭으로 교체. 테스트 25/25. **강제 푸시가 하네스 승인 필요해 1차 시도 차단** → 사용자 "안전하게 동작할 수 있다면 허락한다" 승인 후 재시도 중 원격이 그새 `log-rotate.yml`(00:20 KST 자동)로 08-14 항목을 archive로 이관한 것을 발견 — lease 실패 대신 원인 규명(순수 rotation, 콘텐츠 유실 아님 확인) 후 원격의 post-rotation log.md를 반영해 재작업, 이 항목도 그 위에 append. **② CXL 브랜치 격리**: `cxl-report-sender.yml`이 `on: push: branches: [claude/ai-agent-impl-002tip]`로 **이 저장소의 트랙 A 브랜치를 직접 watch**하던 실결합 발견 — 트랙 A 작업 중 우연히 `wiki/cxl-*.html` 패턴 파일을 건드리면 CXL 메일이 발송될 수 있었음. `fix/cxl-report-sender-checkout-ref`를 정본으로 지정, 트리거를 자기 브랜치 이름으로 교체 + workflow_dispatch(회사 LLM이 ref 지정 API 호출) 유지 + 헤더에 격리 근거·머지 금지 경고 추가. 비파괴적 fast-forward push로 즉시 반영 완료. 나머지 2개 구버전 브랜치(`feat/cxl-report-sender-to-main`, `feat/cxl-report-sender-workflow`)는 남겨둠 — 삭제는 사용자 승인 필요. **③ API 재사용성 검토**: `collectors/*.py`(ECOS/FRED/KOSIS/MOLIT)는 이미 `fetch_series(key)`/`fetch_all()`로 통일된 좋은 패턴. 반면 `scripts/investor_flow.py`·`scripts/portfolio_holdings.py`가 **KIS 토큰 발급 로직을 각자 중복 구현**(이미 07-28~30 403 충돌 실장애의 원인으로 기록됨) + `scripts/macro_data.py`가 `collectors/ecos.py`와 **별도의 ECOS 클라이언트**를 갖고 있어(이번 페이지네이션 버그가 한쪽만 고쳐져 있던 근본 원인) 통합 권고 — 다음 우선순위로 기록, 이번 세션에서 구현은 보류. **④ 스케줄러 확인**: `on: schedule: cron:`이 GitHub Actions의 표준 스케줄러이며 이미 main 6개 워크플로가 사용 중(macro-data-sync 07:10·portfolio-holdings-sync 19:10·real-estate-sync 03:00·subscription-monitor 30분마다·sec-edgar-capex 주1회·log-rotate 00:20). 유일한 제약: cron은 **main의 파일만** 발화 — ①의 main 반영이 완료되는 순간부터 `daily-peos-report`·`core10-collect`가 매일 자동 발화 시작.

2026-08-15 — FEAT(core10-collect 재시도-신선할때까지 루프 구현 + 수동 캠페인 시작) → PR #67 머지 후 core10-collect를 수동 dispatch한 결과 KOSIS 4개·ECOS 7개 **전부 실패**(FRED만 22/22 성공, 1/12). 로그 직접 확인 결과 401/403이 아니라 **TCP connect timeout**(kosis.kr 10초, ecos.bok.or.kr 20초, 3회 재시도 전부) — 서버가 응답을 아예 안 함. 대조군: 9시간 전(08-14 22:37 UTC) `macro-data-sync.yml`은 같은 ecos.bok.or.kr을 **5초 만에 success**로 통과 → 영구 차단이 아니라 일시적 장애/혼잡으로 판정. 사용자 지시 "각각의 데이터가 확보될 때까지 10분 간격으로 계속 재시도 → 신선도 유지, 수동으로 loop 시작, 1시간마다 내가 확인"에 따라 구현. **설계**: `.github/workflows/core10-collect.yml`에 `workflow_dispatch` 입력 `retry_until_fresh`(기본 false — 매일 06:30 스케줄은 영향 없음) 추가, true면 수집+커밋을 하나의 루프로 묶어 최대 30회 반복 — KOSIS/ECOS는 독립적으로 죽고 살아나므로 한쪽이 먼저 복구되면 그 즉시 커밋(전체 성공을 기다리지 않음). 정지 조건: 해당 시도의 자체 성공표에 kosis:/ecos: ❌가 하나도 없으면 즉시 break. **타이밍 설계에서 실측 기반 수정 1건**: "10분 간격"을 시도 종료 후 +600초로 구현하면, 전부 실패하는 시도 자체가 이미 ~9분(관측: run 31871454377, 07:15:11~07:24:18) 걸리므로 실제 간격이 ~19분이 되어 6시간 상한(GitHub 호스티드 러너 하드 캡, 요청값과 무관)에 17회밖에 못 들어감 → 시도 **시작 시각 기준**으로 목표 600초를 채우도록 수정(`remaining = INTERVAL - elapsed`), 30회×10분 ≈ 5시간이 안전하게 들어가도록 재계산. `timeout-minutes: 340`을 백스톱으로 추가. 로컬에서 축소 시뮬레이션(6초 간격)으로 정지조건·적응형 sleep 로직 검증 완료. 테스트 25개 전부 통과(무관 영역이나 회귀 확인). **브랜치**: `claude/ai-agent-impl-002tip`(main 기준)이 아니라 새 브랜치 `claude/core10-retry-until-fresh`에 구현 — "브랜치 우선" 원칙 준수, main엔 아직 안 올라감. **실행**: `ref=claude/core10-retry-until-fresh`, `retry_until_fresh=true`로 workflow_dispatch → run #2(31876846844) queued. 검증되면 이 워크플로 변경을 PR로 main에 올릴 것(사용자 확인 후). **다음**: 1시간마다 이 세션이 직접 확인(run 상태 + 실제 데이터 신선도)해 보고 — send_later로 예약.

2026-08-15 — NOTE(KOSIS/ECOS 주말 접속장애 — 원인 가설 보강, 재시도 설계 그대로 유효함 확인) → 사용자가 외부 조사 결과를 공유: KOSIS·ECOS는 원칙적으로 24시간 운영이나 **금요일 밤~일요일 사이 정기/긴급 점검**(서버 교체·DB 점검·데이터 마이그레이션)이 흔하고, 점검 중엔 API 포함 전 서비스가 막힘. **오늘(2026-08-15)이 토요일**임을 확인 — 지금까지의 관측과 정확히 들어맞음: ①금(08-14) 22:37 UTC macro-data-sync는 ECOS 5초 성공(점검 전) ②토(08-15) 07:14 UTC core10-collect는 KOSIS·ECOS **전부** connect timeout(점검 중, 인증 단계 진입도 못 함 — 401/403이 아니라 TCP 자체가 안 열린 것과 일치) ③토 09:23 UTC 재시도 캠페인 attempt 1에서 **ECOS 3개 시리즈만 성공**(2018-08-20→2026-08-14 복구), KOSIS는 여전히 막힘 — 점검이 전면 차단에서 간헐적 개방으로 넘어가는 전형적 패턴. **결론 — 기존 설계 변경 불필요**: 10분 간격 재시도가 "간헐적으로 풀리는 순간을 포착"하는 정확한 전략(오늘 ECOS가 실제로 그렇게 복구됨)이고, 5시간 상한은 "주말 내내 막혀 있으면 무한 루프 대신 알림"이라는 올바른 경계 — 점검이 일요일까지 이어져 5시간 안에 KOSIS가 못 돌아와도, 내일 06:30 KST 정규 실행이 다시 시도하고 실패 시 오늘 구현한 자동 재-에스컬레이션(`Auto-escalate to a retry campaign`)으로 이어짐. **향후 진단 가이드**: KOSIS·ECOS가 **동시에 전부** connect timeout(응답 자체가 없음, 401/403 아님)으로 실패하고 **금~일 사이**라면 우선 정기점검을 의심 — 코드·키 문제부터 재조사하지 말 것.

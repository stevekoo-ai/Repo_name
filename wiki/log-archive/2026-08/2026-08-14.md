---
title: Log Archive 2026-08-14
created: 2026-08-14
updated: 2026-08-14
tags: [log, archive]
---

# Log Archive — 2026-08-14

아카이브: `wiki/log.md`에서 로테이션(3인 하이브리드, GitHub Actions 층)이
이관한 2026-08-14 로그 항목. 원본은 append-only 그대로 보존.

## 당일 log

2026-08-14 — FIX(죽은 시리즈가 현재값으로 발행되던 문제 — 신선도 가드 신설) → 사용자 질문 "미국 CPI/PPI 컨센서스 수준인데 우리나라가 금리를 계속 올릴까?"에 답하려 한국 국채 커브를 확인하다 발견. **증상**: 리포트가 "한국 10Y 1.56%, 한-미 금리차 312bp"를 현재값으로 실었으나 그 1.56%는 **2018-08-24 관측값(2,912일 경과)** — 존재하지 않는 금리차를 8년 묵은 숫자로 만들어 2026-08-13 종합 리포트까지 발행했음. **원인**: `engine/rate_analysis/scoring.py`의 `_get_latest_rate()`가 날짜를 보지 않고 마지막 행을 반환(`window_days` 파라미터는 선언만 되고 미사용). **범위**: `data/normalized/`의 ECOS 시리즈 3개가 2018-08-24에 멈춤 — `ecos_kr_10y_yield`, `ecos_kr_3y_yield`, `ecos_usdkrw`. 단 환율은 `sources/macro-series.csv` 경로가 별도로 신선(2026-08-12, 1,415원)하므로 리포트 환율값은 정상 — **같은 지표에 신선한 경로와 죽은 경로가 공존**하는 구조라 향후 주의 필요. **조치**: ①`MAX_RATE_AGE_DAYS=45` 가드 신설, 초과 시 값을 버리고 `_STALE_SERIES`에 시리즈명·관측일·경과일 기록 → payload `rate_analysis.stale_series`로 노출 ②스프레드 판정문도 값이 있을 때만 출력(전에는 값이 없어도 "주의 필요"가 찍혀 판단처럼 읽혔음) ③**양쪽 렌더러 모두 수정** — `markdown.py`에 사유 블록, `html_new.py`에 `_rate_or_missing()`·`_stale_series_note()` 추가. html은 `.get(k,'N/A')`가 키 존재 시 None을 반환해 **"None%"로 렌더**되던 것도 함께 해결(5차 교훈 "렌더러는 둘" 적용) ④회귀 테스트 3개 추가(신선도 초과 시 값·스프레드 모두 None 보장, 임계값이 분기 이내인지, 두 렌더러의 결측 처리) → 총 18개 통과. **부수 효과**: 금리 총점 65→60. 이전 65점은 가짜 스프레드로 받은 점수가 섞여 있었으므로 하락이 정상이며 회귀 아님. **사용자 질문에 대한 답(리포트 외 대화)**: 통화방어 동기 소멸(원화 1,548→1,415, -8.6%, 8주 중 6주 하락)과 원화 환산 유가(달러 +34.7% vs 원화 +23.1%, 환율이 11.6%p 흡수 / 12주 전 대비로는 원화 기준 -9.9%)를 근거로 추가 인상보다 동결이 자연스럽다고 판단. 단 국채 커브라는 최선의 단서가 죽은 데이터라 확률은 제시하지 않음. **남은 일**: 세 시리즈의 수집 복구는 ECOS stat_code/item_code 점검 필요(`ECOS Table Lookup` 워크플로) — 현재는 "표시하지 않음"까지만 해결.

2026-08-14 — AUDIT(GitHub Actions 수집 자동화 전수 점검 + ECOS 500행 페이지 상한 버그 수정) → 사용자 "API들을 통한 자료 확보가 자동으로 잘 안 되고 실패도 많은 것 같다, 매일 정해진 시간에 잘 동작하도록 점검해달라". **① 스케줄 워크플로 인벤토리 정정**: 로컬 브랜치 파일로 cron을 셌던 것이 오류 — 스케줄은 **main의 파일**이 결정하므로 origin/main 기준 재조사. 결과 `daily-peos-report`·`daily-clock-report`·`sk-hynix-daily-report` 3개가 main에 **존재하지 않음**. **② 원인은 버그가 아니라 정책 결정**: 커밋 8a3a5f3 등 "policy: remove push/email code"로 **사용자 본인이 2026-08-11에 의도적으로 삭제**(daily-peos-report/daily-brief-report/daily-clock-report/sk-hynix-daily-report/subscription-desktop-report + upload_*.py/dispatch*.{py,sh} + corp-network 우회 concept 5종). 리포트·푸시·이메일 계열만 제거하고 **수집 계열 6개(macro-data-sync/portfolio-holdings-sync/real-estate-sync/sec-edgar-capex/subscription-monitor/log-rotate)는 전부 보존**된 일관된 정책 → 복구하지 않고 그대로 둠. **③ 진짜 문제 — 데이터 신선도 감사**: "잡이 exit 0"과 "데이터가 들어왔다"는 다름을 전제로 `data/normalized/` 175개 시리즈 전수 조사 → 정상 168 / 지연 2 / **중단 5**. 중단: `ecos_kr_10y_yield`·`ecos_kr_3y_yield`·`ecos_usdkrw`(전부 2018-08-24, 2,912일), `fred_kr_cpi_oecd`(1,017일), `fred_kr_industrial_production_oecd`(896일). **④ 근본 원인 규명 — ECOS 500행 페이지 상한**: 중단된 ECOS 3개가 전부 `cycle: D`(일별)이고 정상 4개는 Q/M인 점에서 착안. `_fetch_stat()` URL이 `/1/500/`으로 고정돼 있고 `_HISTORY_YEARS=10`이라, 일별 시리즈(10년≈2,520 영업일)는 **가장 오래된 500행만** 반환받아 2016-08 시작 +500영업일 = **2018-08에서 정확히 멈춤**. 실측 검증: 저장된 행 수 513, 범위 2016-07-26~2018-08-24 — 상한과 정확히 일치. 월별(120행)·분기별(40행)은 500 안에 들어가 정상이었음. **조치**: `_MAX_ROWS=10000`으로 상향(10년 일별 2,520행 대비 충분) + **응답이 상한을 정확히 채우면 `collector.ecos_response_truncated` 경고 로깅**(이 부류를 앞으로 자동 검출). ⚠️ 네트워크 차단 환경이라 실 API 검증 불가 — `macro-data-sync` 수동 실행으로 확인 필요. **⑤ 상시 점검 도구 신설**: `scripts/data_freshness_audit.py` — 주기(일/월/분기)를 관측 간격 중앙값으로 추론해 주기별 허용치로 판정, `--strict`로 CI 연동 가능. "성공 여부를 잡이 아니라 데이터로 측정"하는 표준 도구. **⑥ 내 가드의 오탐 수정**: 어제 넣은 "Fail loudly" 스텝이 `origin/main` 하드코딩이라 **브랜치에서 수동 실행하면 항상 실패**(#44~#47이 실제로 이것 때문). `GITHUB_REF_NAME` 기준으로 변경, push 루프의 rebase 대상도 동일하게 수정. **⑦ 회귀 테스트 2개 추가**(페이지 상한이 10년 일별을 담는지, 신선도 감사가 죽은 시리즈를 잡는지) → 총 20개 통과.

2026-08-14 — AUDIT(시크릿 16개 전수 감사 + Core-10 수집 공백 발견·복구) → 사용자 "secret KEY 목록이 모두 잘 연결되어 동작하는지, 안 쓰는 KEY가 있는지 확인". **① 시크릿 배선 감사 — 16개 전부 정상**: 워크플로 `secrets.X` 참조와 코드의 환경변수 읽기를 대조. 초기 스캔에서 KIS 15개가 "코드가 안 읽음"으로 나왔으나 **내 정규식 오류** — `scripts/portfolio_holdings.py`가 `os.environ.get(f"KIS_APP_KEY_{suffix}") or _get_env_or_die("KIS_APP_KEY")` 형태의 **동적 조합 + 헬퍼 경유**로 읽고 있었음(계정별 접미사 GEN/ISP/IRP/DC, 없으면 기본키 폴백). 동적 패턴·헬퍼를 포함해 재검사한 결과 16개 전원 배선 정상, **워크플로가 참조하는데 미등록인 시크릿 0건**, **코드가 요구하는데 미등록인 API 키 0건**. **② 배선이 아니라 산출물로 재판정**: 키별 실제 데이터 신선도 확인 — KIS ✅(portfolio-holdings.csv, 2일) / FRED ✅(3일) / DATA_GO_KR ⚠️(44일, MOLIT 월간 발표 지연 감안 시 정상권) / ECOS ⚠️(44일, 월간 시리즈라 정상권) / **KOSIS ⛔ 산출 파일 0개**. **③ KOSIS 추적 결과 — 구조적 공백 발견**: `KOSIS_API_KEY`는 `kosis-lookup.yml`(수동 진단 도구)에만 전달되고 **스케줄 수집 워크플로 어디에도 없음**. `kosis_*.csv` 0개. 그런데 `payload.py`는 Core-10 중 4개(산업생산·소매판매·CPI·실업률)를 KOSIS 시리즈에서 찾고, 없으면 OECD 미러로 폴백하는데 그 미러 중 2개가 이미 죽어 있음(fred_kr_cpi_oecd 1,017일 / fred_kr_industrial_production_oecd 896일), 나머지 2개도 105일 지연 → **한국 Core-10 중 4개가 살아있는 소스 없음**. 이것이 리포트마다 "지표 대부분 이월"로 나오던 진짜 원인. **④ 근본 원인**: `build_core10_readings()`는 `engine/macro/engine.py`에서만 호출되고 그 진입점은 `python -m engine.report.run` = **08-11에 정책으로 삭제된 daily-peos-report.yml**. 그 워크플로는 (a)리포트 생성·푸시·이메일 (b)Core-10 수집 **두 가지 일**을 했는데 삭제 사유는 (a)뿐이었고 (b)는 부수 피해였음. 살아남은 macro-data-sync는 `scripts/macro_data.py` PRESETS 기반 **시장데이터층(금리·환율·유가)만** 수집하며 KOSIS를 건드리지 않음. **⑤ 조치 — 수집 전용 복구**: `scripts/collect_core10.py` 신설(KOSIS 4 + ECOS 7 + FRED bulk, 소스별 성공/실패 표로 출력, `--strict` 지원). 리포트 코드 임포트 없음. 검증 중 `fred.fetch_all()`을 `bool(got)`으로 판정하면 **네트워크 차단 시 실패 22건도 "22 series ✅"로 나오는** 관대한 판정 발견 → OK 상태 개수로 조이니 정확히 `0/22 series OK`로 표시됨. `.github/workflows/core10-collect.yml` 신설(cron 30 21 * * * = 06:30 KST, macro-data-sync와 시차 분리, 리포트·이메일 참조 0건, rebase 재시도 + 신선도 감사 스텝 포함해 죽은 시리즈가 조용히 통과하지 못하게 함) — **삭제된 발행 워크플로를 되살리지 않고 보존된 수집 워크플로 6개의 패턴을 따름**. 테스트 20개 통과.

2026-08-14 — STRATEGY(운영 방향 위키 확정 + GitHub 동작 실측 감사 — 보안 경계 2트랙) → 사용자 "회사에서 다른 LLM으로 하는 구현은 보안문제가 있어 github·mail 금지, 경제판단 보고서는 문제 없으니 쉽게 받아보게 해달라 / 구독 한 달 더 결재했고 API 자동화 비율을 높여 최적 구성 후 무료 LLM은 간단한 정리만 시키려는 계획을 이해하고 위키에 저장해달라". **① 전략 문서 신설** — `wiki/concepts/automation-strategy-and-delivery-boundary.md`. 전제 A(보안 2트랙: 애매하면 트랙 B로 취급)와 전제 B(유료 기간은 기능 추가가 아니라 "사람이 붙어야만 되는 일"을 없애는 데 쓴다)를 사용자 발언 그대로 인용해 고정하고, 이번 달 성공 기준을 **"에이전트가 개입하지 않은 날에도 보고서가 도착했는가"** 한 줄로 정의. **② 08-11 퍼지의 정체 규명(가장 중요한 발견)** — `origin/main`의 **루트 커밋 자체가** `3946d3b policy: remove push/email code`였음. 즉 main은 그날 이전 이력이 잘린 **새 뿌리로 재작성**된 상태(74커밋). 함께 사라진 것이 회사망 우회 도구(dispatch.sh/dispatch_log.py/upload_brief.py/daily-brief-dispatch/log-commit-dispatch) + 우회 조사 concept 5종 + CXL 회사 산출물 전부 — **스타일 정리가 아니라 보안 목적의 이력 제거**였고 사용자의 이번 지시와 정확히 같은 판단. 어제 감사에서 "정책 결정이니 복구하지 않는다"까지는 맞았으나 **그 정책이 보안 정책이라는 점을 놓쳤음**을 정정 기록. **③ 부수 피해 원칙 도출** — 삭제된 발행 워크플로는 (a)리포트 생성·발송 (b)Core-10 수집 두 일을 겸했고 삭제 사유는 (a)뿐이었음 → "보안으로 무언가를 제거할 때는 그것이 겸하던 **수집 기능을 먼저 분리해 살려둔다**"를 원칙으로 명문화(collect_core10.py가 첫 사례). **④ 잔여 노출 2건 실측 — 사용자 결정 필요**: 저장소가 **public**임을 GitHub API로 확인(`"private": false`). (노출①) main은 깨끗하나 **퍼지 이전 이력을 가진 브랜치가 남아 있어** `wiki/cxl-newsroom-report-2026-08-11-0953.html`(55KB, CXL 30개사 분석)이 `claude/ai-agent-impl-002tip` 경로로 **실제 다운로드됨**을 재현 확인. 동계열 브랜치 `feat/cxl-report-sender-to-main`·`feat/cxl-report-sender-workflow`·`fix/cxl-report-sender-checkout-ref`. (노출②) `config/portfolio.yaml`(실보유 종목·수량·평단·증권사 구분)·`wiki/entities/user-profile.md`·`docs/report.html`이 main에 있어 공개 상태. **⑤ PR #67의 위험 발견** — 내 브랜치는 퍼지 **이전** 424커밋 이력이고 main과 **공통 조상이 없음**(`git merge-base` 없음, main 74 vs 브랜치 424). 현재 상태로 머지하면 CXL·corp·dispatch 계열 파일 20여 개가 main에 **복원되어 08-11 보안 퍼지가 통째로 되돌아감**. 머지 금지, 경제 부분만 남겨 재구성 필요로 판정(P0). 브랜치 재작성·저장소 공개설정 변경·브랜치 삭제는 전부 되돌리기 어려운 outward-facing 조치라 **에이전트가 임의 실행하지 않고 사용자 승인 대기**. **⑥ main 워크플로 인벤토리 확정** — 11개(macro-data-sync 07:10 / portfolio-holdings-sync 19:10 / real-estate-sync 03:00 / subscription-monitor 30분 / sec-edgar-capex 월 06:00 / log-rotate 00:20 + 수동 진단 5개). **리포트를 생성하는 워크플로는 0개** → 08-11 이후 발행된 리포트는 전부 세션 내 수작업. 스케줄은 기본 브랜치 파일만 발화하므로 `core10-collect.yml`도 main에 올라가기 전엔 영원히 안 돎(= 사용자가 요청한 KOSIS 수동 실행 검증이 아직 불가능한 이유). **⑦ 전달 방법 권고** — 이메일 주채널(private 전환 후에도 동작, 시크릿 이미 등록) + `report/` 보조(발행 증거) + **실패 알림**(08-08~10에 3일치가 사라진 걸 아무도 몰랐던 이유가 성공만 알리고 실패는 안 알린 것). Pages는 private 전환 시 중단되나 메일 본문에 SVG 인라인으로 대체 가능. 회사망 우회 경로는 재구축하지 않음. **⑧ 무료 LLM 경계선** — 넘겨도 되는 것(log append·index 링크·서식·lint) / 넘기면 안 되는 것(**숫자를 만들어내는 모든 작업** — 이 저장소 최악의 사고 전부가 이 부류, 워크플로·시크릿·수집기, 판정 임계값). **⑨ ECOS 페이지네이션 재수정** — 어제 `_MAX_ROWS=10000` 상향은 **효과 없는 오답**이었음. ECOS는 요청당 **1000행이 서버측 상한**이라 더 요청해도 오래된 1000행만 옴. 같은 저장소 안의 대조 실험이 결정적: 같은 API·같은 시리즈·같은 키인데 `scripts/macro_data.py`(1000+페이지네이션)는 2026-08-12 1,415원, `collectors/ecos.py`(/1/500/ 고정)는 2018-08-24 1,120.8원. `macro_data.py` docstring에 2026-07-25 실계정 테스트로 같은 버그를 발견한 기록까지 있었음. `list_total_count` 기반 페이지 순회(20페이지 상한)로 교체하고 미완주 시 `collector.ecos_pagination_incomplete` 경고. **테스트도 교체** — 기존 테스트는 상수만 검사해 오답(10000)도 통과시켰으므로, 실제 1000행 상한을 강제하는 가짜 서버로 바꿔 단일 요청 구현은 통과할 수 없게 함. 총 21개 통과. **⑩ 부수 발견** — `wiki/index.md`가 전체 내용 2회 중복(222줄). 이번엔 양쪽 모두에 항목 추가로 대응, 중복 제거는 별건.

2026-08-14 — FEAT(리포트 전달 채널 + 실패 알림 구현 — "도착하지 않은 날 그 사실이 전달된다") → 전략 문서 P2 항목 구현. **문제 정의**: 이 저장소의 실패는 전부 생성이 아니라 **전달**에서 났고 전부 **조용히** 났음(08-08~10 3일치 push 거부 폐기, 08-11 이후 발행 워크플로 부재). 추가로 확인한 구멍 — `daily-peos-report.yml`이 SMTP/GMAIL 시크릿을 `env:`로 받고 있었으나 **발송 스텝 자체가 없었고**, 파이프라인 내부 `_notify_summary()`는 `MIMEText` 평문 요약만 보내므로 **HTML 리포트는 어떤 경로로도 메일에 실린 적이 없었음**. **① `core/notify.py` 확장**: `send_document(subject, html_body, attachments)`를 Protocol에 추가하고 `EmailChannel`에 구현 — `EmailMessage`로 `text/plain` 대체본 + `text/html` 본문 + 파일 첨부(본문은 폰에서 바로 읽기 위해, 첨부는 보관용 — 클라이언트마다 렌더링이 달라 둘 다 보냄). `Date`/`Message-ID`는 기존 `send()`가 이미 붙이던 것을 동일 적용(누락 시 Gmail이 250 OK 후 조용히 폐기하는 사례가 이미 기록돼 있음). `SlackChannel`은 웹훅이 문서를 못 실으므로 헤드라인만 보내고 저장소 사본을 가리키게 **의도적으로 격하**(50KB 마크업을 채널에 쏟지 않도록). **② `is_configured()` 신설**: `NoopChannel`이 출력만 하고 리턴하므로 **"채널이 없다"와 "보냈다"가 구분되지 않았음** — 이것이 침묵 실패의 구조적 형태. **③ `scripts/send_report_email.py` 신설**: 그날 리포트를 찾아(날짜별 아카이브 우선 → 월 파일 폴백) HTML 본문+첨부로 발송. **설계상 가장 중요한 판단 — 실패 처리를 둘로 갈랐음**: `run.py`의 `_notify_summary()`는 요약 메일 때문에 리포트 생성이 죽으면 안 되므로 **실패를 계속 삼키고**(기존 동작 유지), 이 스크립트는 **전달 그 자체**이므로 채널이 있는데 실패하면 **exit 1**. 로컬용 `--allow-unconfigured` 탈출구 제공. 20MB 첨부 상한(렌더러 회귀 탐지 겸용). **④ `--failure-alert` 모드**: 리포트가 오지 않은 날 사유 + Actions 실행 링크를 메일로 발송. 본문에 "이 메일이 온 날은 리포트를 신뢰하지 마십시오" 명시. **⑤ 워크플로 배선**: `daily-peos-report.yml`에 발송 스텝 + `if: failure()` 알림 스텝 추가(10 스텝), `core10-collect.yml`에도 알림 스텝 추가(7 스텝) — 수집기가 조용히 멈춘 것이 ECOS 3종 2,912일 정지의 형태였으므로 동일 처방. 두 워크플로 상단에 **트랙 A 전용** 경계 주석 명시(회사망 우회 경로 재구축 금지). **⑥ 검증**: YAML 파싱 + 스텝 구성 확인, SMTP를 스텁으로 치환해 실제 리포트(24,431바이트) 발송 경로 통과(메일 68,834바이트), 실패 알림 경로 통과, 미설정 시 exit 1 / `--allow-unconfigured` exit 0 확인. **회귀 테스트 4개 추가**(미설정과 성공이 구분되는지, 미설정 시 조용히 성공하지 않는지, 메일에 HTML 본문·평문 대체본·첨부·Date/Message-ID가 모두 들어가는지, Slack이 원시 HTML을 게시하지 않는지) → 총 25개 통과. **남은 제약**: main에 머지되기 전엔 발화하지 않음(스케줄은 기본 브랜치 파일만 발화).
